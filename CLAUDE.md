# CLAUDE.md — 南関東競馬 予想シミュレーション 3Dデモ

## このプロジェクトは何か
南関東（大井・川崎・船橋・浦和）のレースを、**馬の要素**（脚質・能力・上がり・距離適性・道悪）と
**人の要素**（騎手・調教師・その組み合わせ・馬主の勝負掛け）と**場の要素**（ペース・馬場バイアス・馬場状態・天候）から
モンテカルロで予想し、結果を3Dで再生する静的サイト。

- **公開物は `index.html` 1ファイル・依存ゼロ**（three.js も CDN も使わない。3D は Canvas 2D への自前透視投影）
- **データは別レイヤ**（`data/` + `tools/`）で持ち、ビルド時に index.html へ埋め込む。この2つを混同しない
- 公開先: GitHub Pages（main 直下。push は `tools/publish.mjs` が15分おきにまとめる）。URL は https://kota-aitech.github.io/gamblesimulation/
- UI は日本語。PC は3カラム、幅 900px 以下はスマホUI（下部タブ 3D／予想／条件）

---

## 今後の開発の重要要件（ここは必ず守る）

1. **配布物は1ファイル・依存ゼロを維持する。** npm パッケージも CDN も `<script src>` も足さない。
   データが増えても `index.html` に埋め込む形で解決する（現状 約390KB）。
2. **データはコードに手打ちしない。** 番組・出走馬・前5走・騎手/調教師/馬主の数値は
   すべて `tools/` の取り込みスクリプトが `data/` に落とし、`tools/embed_db.mjs` が index.html に流し込む。
   `index.html` の `/* NKDB:BEGIN */ … /* NKDB:END */` の中は**手で編集しない**（次のビルドで消える）。
3. **推定値には必ず出典と根拠を添える。** 実測（リーディング・出走表）と推定（ability など前5走からの算出）を
   UI 上で区別できるようにする。`race.real` フラグ、各馬の `memo`、右パネルの注記がその役目。
4. **指数はすべて「南関東の平均に対する勝率の対数オッズ差」で統一する。** 0 が平均、+0.7 でおよそ勝率2倍。
   単位を混ぜない。標本の少ない主体は必ず母集団（または上位の予測）へ縮小推定する。
5. **モデルを触ったら必ず2つのチェックを通す。**
   `node tools/sim_check.mjs oi` / `kawasaki`（3角の位置・脚質別勝率・差し馬場の反応）と
   `node tools/ui_check.mjs oi` / `kawasaki`（ブラウザなしで全レースをレンダリングして例外と頭数不一致を検出）。
6. **取得は低頻度・キャッシュ前提。** `tools/lib/nk.mjs` が 600ms 間隔・単一スレッド・ローカルキャッシュを担保している。
   並列取得やキャッシュ無効化はしない。`data/cache/` は `.gitignore` 済み（数百MBになる）。
   **開催前に取ったページを長いTTLでキャッシュしない**こと。結果・払戻・オッズは開催前だと
   空のページが返るので、それを1年抱えると後から永久に取れなくなる（実際にそうなった）。
   `freshTtl(date)` が直近4日を短いTTLにしている。
7. **馬名・成績は公開情報のみ。** netkeiba のコース分析は有料のため使わない。
8. **`.md` の成果物は文字化けするので、こた向けの資料は PDF か HTML で出す。**

---

## 配信先とファイル名（2026-09-17 に変更）
- **3D 予想ページは `sim.html`**（旧 `index.html`）。`index.html` は入口の振り分けだけ（`/` → `top.html`、`?track=` 付きは `sim.html`）。
  この文書で「index.html」と書いてある箇所（NKDB の埋め込み、`lib/model.mjs` が読む本体、sim_check / ui_check）は **すべて `sim.html` のこと**。
- 配信は **GitHub Pages**（main ブランチ直下、`.nojekyll` あり）。リポジトリは 2026-09-18 に `gamblesimulation` に改名。**公開 URL: https://kota-aitech.github.io/gamblesimulation/**（旧 `…/oi-keiba-3d-demo/` は 404。GitHub の転送は git/Web の URL だけで Pages には効かない）。Render は 9/16 に「パイプライン分数の上限」でデプロイが止まったため移行。
  Pages はビルド分数の制限が無い（1時間10ビルドまで）。push は `tools/publish.mjs` が15分おきにまとめる（1日最大96回）。
  Render のサービスは 9/18 に削除済み（`render.yaml` も削除）。

## ファイル構成
```
top.html                **トップ**（サイトの `/` はこれに rewrite）。競馬／ボートの切替タブを持ち、
                        競馬面＝おすすめレース（本命BOX・段位・期待値・自信度）、ボート面＝本命の強い順・締切順・場ごと。
                        選んだ競技は localStorage と `?sport=` に残す ※新聞配色
marks.html              印別の単勝・複勝回収率 ※新聞配色
sim.html                予想シミュレーション＋3D（?track=oi / kawasaki / funabashi / urawa）※ダーク配色。index.html は入口の振り分け
race.html               出馬表（競馬新聞の馬柱。横型／縦型を切替。?track=…&day=…&r=…）※新聞配色
data.html               データブラウザ（騎手・調教師・コンビ・馬主・種牡馬の一覧）※新聞配色
boat.html               ボートレース版（全24場・今日と明日・条件付きロジット。下の専用節を参照）※ダーク配色

data/
  cache/                取得した生ページ（Shift_JIS のまま）。.gitignore 済み・再取得で作り直せる
  nankan/
    leading.raw.json    リーディングの生データ（.gitignore 済み）
    cards.jsonl         出馬表の生データ 1レース1行（.gitignore 済み）
    index.json          ★人的要因データベース（騎手/調教師/コンビ/馬主の指数）— コミット対象
    meet.<track>.json   ★開催日ごとのレース傾向（前方/後方の3着内回数・好走枠）— コミット対象
    trend.<track>.json  ★距離別の傾向とバイアス自動算出の基準値 — コミット対象
    races.<track>.json  ★番組＋出走馬（index.html 埋め込み用）— コミット対象
    entries.<track>.json ★出馬表＋前5走の全項目（race.html 埋め込み用）— コミット対象
    browse.json         ★data.html 埋め込み用にたたんだ全データ — コミット対象
    results.jsonl       ★競走成績（天候・馬場・着順）— コミット対象
    payouts.jsonl       ★払戻金（全券種の組番と配当）— コミット対象
    backtest.json       ★的中率・回収率の検証結果 — コミット対象
    odds.jsonl          ★単勝・複勝オッズ（最終）— コミット対象
    odds_live.jsonl     ★締切前(T-n)と最終の対（追記のみ・検証用）— コミット対象
    odds_pre.json       ★暫定オッズ。1レース1件だけ上書き — コミット対象
    shiken.jsonl        ★能力・調教試験の結果（1頭1行）— コミット対象
    model.json          ★条件付きロジットの係数と検証結果 — コミット対象
    index.train.json    ★先読みを避けた学習用の指数（2023〜2025のみ）— コミット対象

tools/
  lib/nk.mjs            取得共通（Shift_JIS デコード・キャッシュ・レート制限・テーブル読み）
  lib/card.mjs          出馬表パーサ（壊れた HTML を <tr> 分割で読む）
  lib/embed.mjs         HTML のマーカー間に JSON を流し込む inject()
  lib/horse.mjs         1頭ぶんの推定値・指数・寸評（本番と検証で共用）
  lib/model.mjs         index.html のモデルを VM に読み込む loadModel/condOf
  lib/feat.mjs          条件付きロジット用の特徴量39個（レース前の情報だけ）
  lib/bets.mjs          買い目・期待値・自信度（表示と検証で必ず同じ計算を使う）
  fetch_leading.mjs     リーディング取得 → leading.raw.json
  fetch_cards.mjs       出馬表取得 → cards.jsonl
  fetch_trend.mjs       レース傾向取得 → meet.*.json
  fetch_shiken.mjs      能力・調教試験の結果 → shiken.jsonl
  fetch_results.mjs     競走成績取得（天候・馬場・着順）→ results.jsonl
  fetch_payouts.mjs     払戻金一覧取得（1日1リクエストで全券種）→ payouts.jsonl
  fetch_odds.mjs        単勝・複勝オッズ取得（/oddsJS の軽い JS）→ odds.jsonl
  watch_odds.mjs        締切前・暫定のオッズを自動で拾う → odds_live.jsonl / odds_pre.json
  refresh.mjs           オッズが変わったら印・期待値・買い目・TOPを作り直して各ページへ反映
  nightly_results.mjs   夜にその日の結果・払戻・最終オッズを取り込み、日別の成績を更新して push（launchd: com.nankan.results）
  odds_drift.mjs        締切前と最終オッズのズレを測る
  launchd/              締切前オッズ取得を macOS に登録する plist と install.sh
  fit_model.mjs         条件付きロジットの当てはめ → model.json
  backtest.mjs          予想の当て方ごとに的中率・回収率を検証 → backtest.json
  build_db.mjs          指数の算出 → index.json
  build_trend.mjs       距離別傾向・バイアス基準値の算出 → trend.*.json
  build_races.mjs       番組・出走馬の生成 → races.*.json / entries.*.json
  build_marks.mjs       index.html のモデルを VM で回して予想印を作る → entries.*.json 更新＋race.html
  build_browse.mjs      data.html 用に全データをたたむ → browse.json
  build_top.mjs         top.html 用の要約（段位・期待値・買い目）→ top.json
  build_results.mjs     終わったレースの結果と実測 → results.<track>.json
  build_marksrec.mjs    印別の単勝・複勝回収率 → marksrec.json
  race_pick.mjs         段位の閾値決めと「絞ると回収率が上がるか」の検証 → racepick.json
  embed_db.mjs          index.html(NKDB) / race.html(NKRACE) / data.html(NKBROWSE) に埋め込む
  sim_check.mjs         シミュレーションの妥当性チェック（ブラウザ不要）
  ui_check.mjs          DOM スタブで init() を通し、全レースをレンダリング（ブラウザ不要）
  data_check.mjs        data.html の全タブが描画できるかを確認（ブラウザ不要）
  race_check.mjs        race.html の全開催日・全レースを描画して欠落を確認（ブラウザ不要）
```

---

## データパイプライン（いちばん多い作業）

```bash
# 1. 出馬表を取り込む（既に取ったページはキャッシュから読むので追記は速い）
NK_FROM=202509 NK_TO=202609 node tools/fetch_cards.mjs

# 2. リーディング（騎手 / 調教師 / 騎手×調教師）を更新する。年をまたいだとき・月イチでよい
node tools/fetch_leading.mjs

# 3. レース傾向（開催日ごとの前方/後方・好走枠）を取り込む
node tools/fetch_trend.mjs

# 4. 指数と傾向を作り直す
node tools/build_db.mjs && node tools/build_trend.mjs

# 5. 番組・出走馬とブラウザ用データを生成して各ページに埋め込む
node tools/build_races.mjs && node tools/build_browse.mjs && node tools/embed_db.mjs

# 6. 予想印を打つ（embed_db のあと。index.html のモデルを読むので順番を守る）
node tools/build_marks.mjs

# 7. 検証
node tools/sim_check.mjs oi && node tools/sim_check.mjs kawasaki
node tools/ui_check.mjs  oi && node tools/ui_check.mjs  kawasaki
node tools/data_check.mjs && node tools/race_check.mjs oi && node tools/race_check.mjs kawasaki

# 見た目の確認（playwright は入っていない。Chrome のヘッドレスで十分）
CH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
"$CH" --headless=new --disable-gpu --hide-scrollbars --virtual-time-budget=4000 \
  --screenshot=/tmp/pc.png --window-size=1400,1200 "file://$PWD/race.html?track=oi"
"$CH" --headless=new --disable-gpu --hide-scrollbars --virtual-time-budget=4000 \
  --screenshot=/tmp/sp.png --window-size=390,1400 "file://$PWD/race.html?track=kawasaki"
python3 -c "import re;open('/tmp/c.js','w').write(re.findall(r'<script>(.*?)</script>',open('index.html',encoding='utf-8').read(),re.S)[-1])" && node --check /tmp/c.js
```

環境変数
| 変数 | 既定 | 意味 |
|------|------|------|
| `NK_WAIT` | 600 | 取得間隔(ms) |
| `NK_FROM` / `NK_TO` | 直近12ヶ月 | 出馬表を取る年月 (YYYYMM) |
| `NK_PERIODS` | `2023,2024,2025,2026,0003,0004` | リーディングの期間。`0003`=直近1年 `0004`=直近3ヶ月 |
| `NK_RACE_DAYS` | 2 | index.html に載せる開催日数（増やすとファイルが重くなる） |
| `NK_RACE_TRACKS` | `大井:oi,川崎:kawasaki` | 生成する場 |
| `NK_MEET_DAYS` | 14 | レース傾向を取る直近の開催日数 |
| `NK_SIRE_DIST` | `1200,1400,1500,1600,1800,2000` | 種牡馬の距離別を取る距離 |
| `NK_MARK_TRIALS` | 600 | 予想印を出すモンテカルロの試行回数 |
| `NK_TODAY` | 今日 | 「今日を含む開催」の基準日。過去日を再現したいとき |

### データ元（nankankeiba.com）
| 用途 | URL |
|------|-----|
| 開催日カレンダー | `/calendar/{YYYY}{四半期の先頭月}.do`（例 202607 が7〜9月） |
| 番組表 | `/program/{YYYYMMDD}{場2}{回2}{日2}.do` |
| 出馬表 | `/uma_shosai/{その14桁}{R2}.do` … 馬主・騎手・調教師・**前5走の全項目** |
| 結果 | `/result/{同16桁}.do` |
| 払戻 | `/repay/{開催日14桁}.do`（1日1リクエストで全レース） |
| リーディング | `/leading_{kis\|cho\|kis_cho\|sire\|bms}/{場2}{距離4}{馬場2}{年4}{並び2}{種別1}.do?PAGE=n` |
| レース傾向 | `/race_trend/{YYYY}{場2}{回2}{日2}.do?open_date=YYYYMMDD` |

### 取れるもの・取れないもの（調査済み）
| 欲しいもの | 可否 | 備考 |
|---|---|---|
| 出馬表（前5走こみ） | **2024-01-01 以降のみ** | 2023-12-31 以前は簡易版になっていて出走馬が載らない。学習期間の下限はここ |
| 結果・ハロンタイム（レースラップ） | ○ | `/result/` に 上がり3F・上がり4F・ハロンタイムが出る |
| 単勝・複勝オッズ | ○ | `/oddsJS/{raceId}.do` が1KB弱で全頭ぶん |
| 払戻金（全券種） | ○ | `/repay/{開催日}.do` が1日1リクエストで全レース |
| 格・格付ポイント・収得賞金 | ○（ただし現在値） | `/uma_info/{horseId}.do`。過去時点の値ではないので学習にそのまま使うと先読みになる |
| **調教（追い切りタイム）** | **×** | 南関は公開していない |
| **能力・調教試験** | ○ | `/shiken_list/{YYYYMMDD}{場2}.do`。一覧ページは直近10回ぶんしかリンクが無いが、**URLを日付から組み立てれば直接開ける**。2024-01 まで遡れて2,394頭ぶん取れた。馬IDでリンクされているので突き合わせは確実 |

場コード `18`浦和 `19`船橋 `20`大井 `21`川崎、`00`南関東（＝4場の合計）。
種別 `1`騎手 `2`調教師 `3`サイアー `4`BMS `5`騎手×調教師。並び `01`勝利数。
種牡馬・母の父の表には2着3着が無い（出走回数・出走頭数・勝馬頭数・勝利数・勝率・勝馬率・収得賞金のみ）。
ページは Shift_JIS。出馬表は `</tr>` が欠けた壊れた HTML を返すので `lib/card.mjs` の読み方を踏襲する。

---

## 人的要因データベース（`data/nankan/index.json`）

南関東4場の**過去3年＋今年（2023〜2026、約136,000走）**の全出走が母集団。平均勝率 9.0%／3着内率 27.0%。

### 指数の定義
| キー | 対象 | 中身 |
|------|------|------|
| `jockey[kisXXXXXX].idx` | 騎手 299人 | 勝率の縮小ロジット（k=60騎乗）。`0.7×3年 + 0.3×直近1年` |
| `jockey[].byTrack` | 場別 | 本人の全体指数を事前分布に置いた場別の指数 |
| `jockey[].hot` / `form` | 調子 | 直近3ヶ月／直近1年 の3年平均からのズレ |
| `trainer[choXXXXXX].idx` | 調教師 400人 | 同上（k=60出走） |
| `combo["kisA\|choB"].cIdx` | コンビ 7,600組 | **騎手・調教師から独立に期待される勝率からの上振れ**（k=45出走） |
| `combo[].pairIdx` | | `wJ×騎手 + wT×調教師 + cIdx`＝このコンビの総合力 |
| `combo[].bond` | **結び付き** | その厩舎の全出走に占めるこのコンビの割合。0.30以上＝主戦、0.12以上＝準主戦 |
| `combo[].bondJ` | | 騎手側から見た割合 |
| `owner[馬主名].oIdx / oIdx3` | 馬主 約1,000名 | 所有馬の勝率／3着内率の縮小ロジット（k=40） |
| `owner[].oAgg` | **勝負傾向** | 起用した騎手の平均指数が全馬主平均よりどれだけ上か |
| `sire[].idx` | 種牡馬 548 | 勝率の縮小ロジット（k=80出走）。`0.75×3年 + 0.25×直近1年` |
| `sire[].byDist` | 距離別 | 南関東まとめの距離別。**本人の全体指数を引いた「距離での偏り」**（20走未満は載せない） |
| `bms[].idx` | 母の父 892 | 同上 |
| `fit` | | 下記 wJ / wT / 切片 |

### 係数 wJ / wT は当てずっぽうにしない
騎手指数と調教師指数は相関する（強い厩舎ほど上位騎手が乗る）ので、単純な和では過大評価になる。
`build_db.mjs` は全コンビの (出走数, 勝利数) に対して
`勝率のロジット = L0 + c + wJ×騎手指数 + wT×調教師指数` の二項尤度をグリッド探索で最大化し、
現在は **wJ = wT = 0.80、切片 −0.05** を得ている。コンビ指数はこのモデルの残差。
データを更新したら係数も動く。**手で書き換えず、必ず `build_db.mjs` の出力を使う。**

### 勝負掛け指数 `shobu`（レースごとに `build_races.mjs` が算出）
```
jUp   = 今回騎手の指数 − 前走騎手の指数        （乗り替わりの強化度）
spot  = この厩舎で普段乗らない上位騎手か        （bond < 0.08 かつ 騎手指数 > 0.30 で 1）
away  = 調教師・騎手の所属が開催場と違うか       （各 0.5）
oAgg  = 馬主の勝負傾向

shobu = 0.55×jUp + 0.35×spot×騎手指数 + 0.30×oAgg + 0.12×away
```
前走騎手は出馬表の前走欄が3文字略称（「御神訓」）なので、**部分列一致**で騎手マスタに突き合わせている。

### 走りへの効かせ方（`index.html` の `simRace`）
```
hIdx = 0.8×騎手 + 0.8×調教師 + コンビ上振れ + 0.20×馬主 + 0.30×勝負掛け
hx   = clamp(hIdx, -1.6, 2.6) × cond.human      // cond.human は左パネルのスライダー 0〜2、既定 1.0
  ke   += hx*0.0012     出脚
  kl   += hx*0.0040     終いの伸び（馬の ability の約1/3の重み。人が馬を上回らないようにしている）
  mult *= 1 + hx*0.0015 基礎スピード
  base -= hx*0.15       進路取り（上手い騎手ほど内を通す）
```
**係数を上げすぎると人的要因だけで決着してしまう。** 変更したら `sim_check.mjs` の「人0%／人1%／人2%」列で、
上位馬の勝率が2倍以上に跳ねていないかを見る。

### 能力・調教試験（`skTime` / `skHas` / `skFail`）
新馬・転入初戦は前5走が無く、血統しか手がかりが無い。試験タイムは**その馬が実際に走った唯一の記録**。
`/shiken_list/{YYYYMMDD}{場2}.do` から取る。表は rowspan で母父が2行目に来る壊れた組み方なので、
`<tr>` ではなく「`uma_info` リンクから `</tr>` まで」の塊で読む。

`skTime` は「試験タイムの中央値からの速さ」を、前走数が少ないほど強く効かせる（3走以上あると0）。

**効果は限定的だった。**
- 相関はある：初出走1,606頭で、試験タイムと初戦の相対着順の相関 r = −0.174。
  速い1/4は着順スコア0.643、遅い1/4は0.437とはっきり差がある
- ところが**係数は +0.024 と小さい**。学習データ10万頭のうち前走なしは2.7%、
  そのうち試験タイムがあるのは63%で、**全体の1.7%にしか効かない**ため
- 新馬・初出走が過半のレース91件で測ると logloss 1.646 → **1.611** と改善するが、
  上位3頭に勝ち馬は 78.0% → 75.8% と下がる。**予測精度としては誤差の範囲**

→ **モデルの数字より、寸評に出す情報としての価値のほうが大きい。**
新馬戦の寸評は「南関の前走なし」だけだったのが、
`【能力試験】08/21 川崎 51.4秒 合格（平均比 -1.2秒・速い）／421kg` と比較できるようになった。
`brief` にも `試験51.4秒(速)` を出す。

### 騎手・調教師の調子（`jForm` / `jForm3` / `jRides` / `tForm`）
`index.json` の `hot` は build_db を回した時点の直近3ヶ月なので、過去のレースに当てると先読みになる。
`buildFormIndex()` が結果を日付順に流し込み、**各レースについて「その日より前の騎乗だけ」**から作る。
未来のレース（結果がまだ無い）は最新の状態を使う。

**作り方を間違えると符号が反転する。** 生の直近成績は長期指数 `jIdx` と相関0.81でほぼ同じ情報になる。
かといって単純に `jIdx` を引くと、縮小のかかり方の差が残って**逆相関の見せかけ**が出る
（好調な1/4のほうが着順が悪い、という結果になった）。
正しくは **その騎手自身の平常値を事前分布に置いて縮小**する（`fm.get(raceId, 'j', id, jIdx)`）。
そうすると `jIdx` との相関が 0.255 に落ちて、着順との相関は +0.081 になる。

**効果は小さい**（`jForm` −0.027 / `jForm3` +0.045 / `jRides` +0.023 / `tForm` −0.016）。
好調な1/4の着順スコアは0.509、不調な1/4は0.455で差はあるが、`jIdx` でほぼ説明がついてしまう。
→ **モデルより寸評での価値のほうが大きい**。「増田充宏は直近60騎乗が不振」のように出す。

### 馬体重（`bwDev` / `bwSwing` / `bwRel` / `bwLog` / `bwDiff`）
**当日発表**なので、前日までは空。`refresh.mjs` が開催中30分おきに当日ぶんの出馬表を取り直す
（`NK_ONLY_DATE`）。**当日は `fetch_cards` のTTLを10分にしないと取り直せない**（1日だと固まる）。
同じ raceId が二重にならないよう、取り直しは追記ではなく差し替えにしている。

| 特徴量 | 中身 | 係数 |
|---|---|---|
| `bwLog` | 馬格（460kg基準の対数） | **+0.162** |
| `bwRel` | 同じレース内での相対 | −0.078 |
| `bwDiff` | 前走からの増減 | −0.076 |
| `bwDev` | **その馬の普段の体重との差**（前走欄の馬体重から算出） | +0.062 |
| `bwSwing` | 増減の絶対値（大幅増減は減点） | −0.030 |

`bwLog` は追加前の +0.080 から **+0.162 に倍増**した。自己比・相対を入れたことで
「大型馬が有利」と「仕上がりが良い」を切り分けられるようになったため。
寸評には `【馬体】490kg（+4）／平均476kg・普段より重め` と出す。

### 血統の効かせ方
`bIdx = (父の指数 + 0.45×父のこの距離での偏り + 0.35×母の父の指数) × max(0, 1 − 前走数/4)`
**前5走がそろっている馬では 0 になる**（実績がすでに血統を織り込んでいるため）。
2歳戦・新馬・転入初戦のように手がかりが少ないときだけ効く。`simRace` では `kl += bIdx*0.0040`。

---

## ページ構成
4ページとも**それぞれ1ファイル・依存ゼロ**。共通のデータは `tools/embed_db.mjs` が各ファイルの
マーカーに埋め込む。ページをまたぐ共通 JS ファイルは作らない（作ると依存ゼロが崩れる）。

| ページ | マーカー | 埋め込むもの | 重さ |
|---|---|---|---|
| `index.html` | `NKDB` | `races.*.json` + `trend.*.json` | 約525KB |
| `race.html` | `NKRACE` | `entries.*.json`（前5走・予想印こみ） | 約1.0MB |
| `data.html` | `NKBROWSE` + `NKBT` | `browse.json` + `backtest.json` | 約1.1MB |
| `top.html` | `NKTOP` ＋ `NKBOATTOP` | `top.json`（南関）＋ `data/boat/top.json`（ボート） | 約155KB |
| `marks.html` | `NKMR` | `marksrec.json` | 約15KB |

### 見た目の方針
`race.html` と `data.html` は**競馬新聞の紙面**に寄せてある。共通の決めごと：

- 配色は紙（`--paper #fbf8f1`）に黒インク。赤 `--red #c0102a` と青 `--blue #1b4f9c` だけをアクセントに使う
- 馬名・見出し・着順は**明朝**（`--mincho`）、数値と細字はゴシック（`--gothic`）
- 枠色は競馬の標準（1白 2黒 3赤 4青 5黄 6緑 7橙 8桃）。`race.html` の `WAKU` に背景と文字色の組で持つ
- 罫線は細く多く。題字と表頭は黒帯に白抜き
- 実在紙（ダービーニュース等）の名前・ロゴ・紙面画像は使わない。**様式だけを真似る**
- `index.html` は3Dの舞台が暗くないと成立しないのでダーク配色のまま

**公開後のデータの場所**：`data/nankan/*.json` はリポジトリ直下から静的配信されるので、
`https://<サイト>/data/nankan/index.json` のように直接ダウンロードできる。
このために `render.yaml` から SPA 用の catch-all rewrite を外してある（戻すと JSON も
HTML も index.html に書き換わりうる）。

---

## おすすめレース・期待値・自信度

### 定義（`tools/lib/bets.mjs`。表示と検証で必ず同じ計算を使う）
- **期待値**：単勝オッズから Harville で組み合わせの市場価格を推定し、控除率25%を引いたうえで
  モデル確率と比べた倍率。1.00 が損益トントン
- **自信度**：`1 − 正規化エントロピー`。1頭に確率が集まっているほど大きい
- **段位 S/A/B/C**：馬連の期待値の分位（S＝上位1割、A＝上位1/4、B＝上位半分）。
  閾値は `racepick.json` が検証データから決める
- **おすすめ買い目**：期待値1.0超の組み合わせのみ。ただし Harville は極端な低確率の組で
  分母が潰れて想定配当が数十万倍になるため、`LIMIT`（モデル確率の下限・想定配当の上限）で
  現実的な範囲に絞る。**この制約を外すと「買えない買い目」で期待値が決まってしまう**

### 何をおすすめとして出すか（**重要**）
実測（大井110R＋川崎120R＝230レース、1点100円）：

| 買い方 | 大井 | 川崎 |
|---|---|---|
| 本命BOX 上位4頭・三連複 | **112%** | 84% |
| 本命BOX 上位3頭・三連複 | 92% | 81% |
| 本命BOX 上位4頭・馬連 | 77% | 76% |
| 期待値1.0超だけ買う・馬連 | **0%**（110Rで的中0本） | 50% |
| 期待値1.0超だけ買う・三連複 | 52% | 47% |

**期待値1.0超だけを買うやり方は使い物にならない。** 市場と食い違う組だけを買う逆張りになるため、
モデルが市場を上回っていない現状ではただの外れ馬券になる。
**おすすめの主役は本命BOX**にして、期待値の組は「参考」として実績つきで併記する。
画面には必ず実測の回収率を並記すること（`NKR.record` / `NKT.record`）。

### 場別の成績（`top.html` の「場別の成績（通算）」。2026-09-18 追加）
南関（4場）・中央（10場）・ボート（24場）で、**実際に記録した予想を払戻で精算した通算**を場ごとに並べる。ばんえいは帯広だけなので出さない。
- データ：`results.json`（ボート／中央）に `byVenue`（全期間の場別合計。`build_boat_results.mjs` / `jra_build_results.mjs` が日別と同時に積む）、
  南関は `results.<track>.json` の `meta.summary` がそのまま場別（`build_top.mjs` が `record` に入れる）
- 検証側も場別を持つ：`backtest_boat.mjs` の `byVenue`（◎単勝・3艇BOX3連複・4艇BOX3連複）、`jra_backtest.mjs` の `byVenue`（同）、南関は `backtest.json` の `tracks`
- 画面：`top.html` の `venueBoard(selEl, bodyEl, rows, metrics, opts)` が3競技で共通。買い方のボタンで並べ替えと棒が変わる。
  列は 場／R／◎的中／上位3／**回収率（数値）**／棒（細線が100%）／的中／検証の同型。棒は 100%以上が赤・70%未満が灰
- **表のセルの中では `<div>` の幅が 0 に潰れることがある**（`table.sum` の中で実測 2px になった）。棒には `min-width` を直接持たせること
- 場ごとのレース数は少ない（ボートで1場あたり12〜72R）。**場の優劣として読ませない**注記を必ず出す

### 日別の成績（`top.html` の「日別の成績」）
`build_results.mjs` が `summary.byDay` を作り、TOPに開催日ごとの表を出す。
**結果の取り込みと出馬表の取り込みは夜に自動**（launchd `com.nankan.results` → `tools/nightly_results.mjs`。21:20／23:00／翌06:30 に
昨日〜今日の 払戻・結果・最終オッズ を取って `build_results`、続けて **今月の出馬表を取り直し**
（`fetch_cards → fetch_trend → build_trend → build_races → build_browse → embed_db → build_marks`）、`build_top → embed_db → commit/push`）。
出馬表は前日夕方に出るので 21:20 の回で翌日ぶんが入る。手で回すのは月またぎで `NK_FROM/NK_TO` を変えるときだけ。
当日でも結果と払戻が取れていれば日別に入る（`c.date <= TODAY`）。ログは `data/nankan/results.log`。
◎的中／上位3頭に勝ち馬／各BOXの回収率と的中率／**その日に使ったオッズの種類**を並べる。

**開催中の結果は `refresh.mjs` が8分おきに取る**（2026-09-17〜）。発走10分後から、終わったレースだけ `fetch_results`／`fetch_payouts` を今日の日付で回し
（`NK_RES_AFTER_POST=10`。取得済みは飛ばすので夜の nightly と二重にならない）、増えていれば `build_results` を回して「開催の結果」と日別の成績を日中に更新する。
JRA・ばんえいの結果は各 refresh が1日1回、ボートは締切6分後から公式サイトの結果ページ（`fetch_live`）。

**「使ったオッズ」の列を必ず見ること。**「最終」の日はレース後に確定したオッズで予想を再現しており、
実戦より有利に出る。**「締切前」の日だけが実運用に近い数字**。
2026-09-09 が締切前オッズで11/12レースを予想できた最初の日。

日別は12レースしかないので、1本の高配当で回収率が100ポイント動く。合計のほうを見る。

### 印別の単勝・複勝（`marks.html`）
945レースでの実測。**どの印も100%に届いていない。**

| 印 | 勝率 | 複勝率 | 単勝回収 | 複勝回収 |
|---|---|---|---|---|
| ◎ | 41.8% | 73.0% | 77% | 85% |
| ○ | 21.9% | 57.3% | 85% | 81% |
| ▲ | 12.4% | 44.1% | 76% | 76% |

順位付けとしては機能している（◎の複勝率73%）が、オッズに見合うところまでは行っていない。

### 絞り込みは効くのか（**重要**）
536レース（2026-06〜09）を前半268Rで閾値を決め、後半268Rで試した結果：

| 絞り方 | 後半のレース数 | 馬連4頭BOX | 三連複4頭BOX |
|---|---|---|---|
| 期待値（馬連）上位1割 | 33R | **103%**（全体77%） | 95%（全体93%） |
| 期待値（馬連）上位1/4 | 61R | 90% | 81% |
| 期待値（三連複）上位1割 | 27R | 57% | **49%** |
| 自信度 上位1割 | 13R | 100% | 106% |

**絞り込みで回収率が安定して100%を超える証拠は得られていない。**
指標や絞り具合を変えると49%〜106%まで振れる。レース数を減らすほど数字が暴れるだけとも読める。
**段位を「買う・見送る」の機械的な根拠にしてはいけない。** 金額の強弱をつける参考値まで。
この但し書きは top.html と race.html の画面にも出してある。**外さないこと。**

---

## 出馬表ページ（`race.html`）— 競馬新聞の馬柱
**出馬表に出すのは今日以降のレースだけ。** 終わったレースは `build_results.mjs` が
`results.<track>.json` に落とし、結果として見せる（開催の谷間だと0レースになるが、それが正しい状態）。

1頭＝1行の馬柱。左から **印／枠・馬番／馬名ブロック／本紙指数／前5走×5コマ**。

- 馬名ブロックは新聞と同じ積み方：父 → 馬名（性齢・斤量）→ 母（母の父）→ 脚質・騎手・厩舎 → 馬主
- 前5走の1コマは 着順・場・日付／馬場・距離／レース名／頭数・馬番・人気／騎手・馬体重／
  上がり3F（順位は丸数字）／コーナー通過順。1着は赤、2〜3着は青
- 上の「前5走・指数・寸評」で列ごと出し入れできる（`body.no-runs` などのクラスで CSS が列幅を切り替える）
- 出走取消馬は薄く表示する（頭数には数えない）

### 開催の結果（`?view=results`、2026-09-16）
ボートの「節間の結果」と同じ見せ方。masthead 下の「出馬表／開催の結果」で切り替える（`setView`。`[hidden]{display:none!important}` が無いと
`display:flex` の要素が隠れない）。データは `lib/nkresults.mjs` の `raceExtras(TRACKS)` が `results.<track>.json` から組む
（`record`＝日別の集計、`results`＝開催ごとのレース行）。**build_marks と embed_db の両方が race.html の NKRACE を書く**ので、
どちらもこの関数を通す（片方だけだと反映の順序で消える。実際に消えた）。
- 開催（回）は「第n日」が 1 に戻る／減るところで区切り、直近2開催ぶんを埋める（4場で約125KB）
- 1行＝着順（1〜3着は枠色つき、4着以下は番号）・三連単配当と人気・三連複配当・締切時点の印 ◎○▲△ と ◎の勝率・使ったオッズ（締切前／最終）・
  的中の札（◎1着・上位3頭・3頭BOX馬連・3頭BOX三連複・4頭BOX三連複）。本命BOXの的中と払戻は `raceExtras` が計算（日別の成績と同じ買い方）
- 日の集計（確定数・◎1着・上位3頭・3頭／4頭BOXの回収と本数）を上に出す。TOP の「日別の成績」から各場へリンク

### 縦型（競馬新聞の組み方）
「縦型」ボタンで 1頭＝1本の柱に切り替わる（`body` ではなく `#board.tate` で切替、選択は localStorage に保存）。

**並びは競馬新聞と同じく右から1番。** ただし `direction:rtl` ＋ `flex-direction:row-reverse` で
やってはいけない。左にはみ出した柱がスクロールで拾えず、**14頭立てで1頭が画面外に消えた**。
正しくは `hs.slice().reverse()` で配列を逆順にして通常の LTR で並べ、
描画後に `board.scrollLeft = board.scrollWidth` で右端（1番）へ寄せる。

**幅は CSS ではなく JS（`fitPillars()`）で決める。** ここは2回踏んだので理由を残す。
- `justify-content:flex-end` を使うと、**あふれた分が左側に出てスクロールで拾えなくなる**
  （14頭立てで12番より左に行けなくなった）。使わない
- `width:max-content` ＋ 柱の `max-width` を併用すると container がそこまで伸びて左端が切れる
- 正解は `.pillars{width:max-content}` の左詰めのまま、`fitPillars()` が
  `頭数×既定幅 < board.clientWidth` なら `--pw = clientWidth/頭数` に広げ（余白が消えて1番が右端）、
  あふれるなら既定幅のままにして `scrollLeft = scrollWidth`（右端＝1番から見せる）。
  resize でも呼ぶこと
- 直したら Chrome で `scrollWidth - clientWidth`（スクロール可能量）と
  左端までスクロールしたときに1番目の柱が見えるかを実測する。
  DOM スタブの `race_check.mjs` では幅を測れないので、ここだけは実ブラウザで確認する

`race_check.mjs` が柱の本数と出走頭数の一致を見ているので、崩すと検出される。
- 柱の中身は上から 印／枠・馬番／父・母父／**馬名（縦書き）**／性齢・斤量・脚質・騎手・厩舎／
  本紙の勝率・3着内率／指数チップ／前5走／寸評
- 馬名は `writing-mode:vertical-rl; text-orientation:upright`。長い馬名は文字数で
  クラス（`l9`/`l11`/`l13`）を付けて字を詰め、**必ず柱に収める**（`nameCls()`）
- スマホは柱を104px幅にして横スクロール

### 展開予想
レース見出しの下の帯。**シミュレータが出した3角の平均位置（`h.c3`）で並べ、4つの集団に分ける**。
逃げ＝3角2.5番手以内、残りを3等分して 先行／中団／後方。左ほど前。
予測はロジットに移したが、**位置取りだけは物理シミュレータのほうが素直**なのでここは `c3` を使う。
`top.html` のカードにも同じ並びを1行（`flow`）で入れている。
`race_check.mjs` が「展開予想の頭数＝出走頭数」を照合する。

**レイアウトの注意**
- `.thead` と `.uma` は**同じ `grid-template-columns`** を使う。片方だけ直すと前走欄がずれる
- 前走の5コマは親グリッドの最終列（`minmax(720px,1.5fr)`）の中で `repeat(5,1fr)`。
  `auto` にすると見出しと行で幅が変わる
- `.thead` に `position:sticky` は付けない。`#board` が横スクロールするため効かない
- スマホ（840px以下）では `.uma` を block にし、`.m-top` を
  「印／枠馬番／プロフィール」の3列グリッド、**指数は `grid-column:1/-1` で次の行に全幅**で置く。
  指数を上の段に混ぜると馬名の列が潰れて1文字ずつ縦に折り返す
- 前5走はスマホでは横スクロール（`scroll-snap`）にする

### 予想印
印と勝率は**別式で作らない**。`tools/build_marks.mjs` が `index.html` のセクション0〜5を
そのまま VM に読み込み、`monteCarlo` を既定の馬場・天候＋傾向からの自動バイアスで回した結果を
`entries.*.json` に書き戻している。**`embed_db.mjs` のあとに実行する**（index.html の傾向データを使うため）。
`autoBias` の式だけは `build_marks.mjs` にも書いてあるので、片方を変えたら両方直す。

---

## 的中率・回収率の検証（`tools/backtest.mjs`）

```bash
NK_BT_TRACKS=大井 NK_BT_FROM=2026-08-31 NK_BT_TO=2026-09-04 node tools/fetch_payouts.mjs
NK_BT_TRACKS=大井 NK_BT_FROM=2026-08-31 NK_BT_TO=2026-09-04 node tools/fetch_results.mjs
NK_BT_TRACKS=大井 NK_BT_FROM=2026-08-31 NK_BT_TO=2026-09-04 node tools/backtest.mjs
node tools/embed_db.mjs      # data.html の「成績・回収率」タブに反映
```
`NK_BT_TRACKS` を変えるだけで他場に横展開できる（浦和・船橋も出馬表を取ってあれば動く）。
`NK_BT_SIZES`（既定 3,4,5,6）で BOX の頭数、`NK_BT_TRIALS`（既定 800）で試行回数。

- 買い方は「予想勝率の上位 k 頭の BOX を1点100円」。券種は 馬連・馬単・三連複・三連単・枠単
- 予想は必ず `lib/model.mjs` 経由で **index.html のモデルそのもの**を回す。検証用の別式を作らない
- 馬場・天候は results.jsonl の**実際に発表された値**を使う（レース前に分かる情報なので使ってよい）
- 比較用に「単勝人気の上位 k 頭 BOX」も同じ条件で出す。**これに勝てていなければモデルに賭ける価値はない**

### 検証結果（学習 2024-01〜2026-05 の 7,931レース／検証 2026-06〜09 の 945レース）

予測精度

| | シミュレータ | ロジット第1段 | 単勝人気だけ | 合成（第2段） |
|---|---|---|---|---|
| logloss | — | 1.855 | **1.638** | 1.659 |
| 1着的中 | 20〜37% | 35.0% | **42.3%** | 41.8% |
| 上位3頭に勝ち馬 | 55.0% | 67.3% | 76.3% | **76.1〜77.4%** |

学習データを1年（2,413R）から2年9ヶ月（7,931R）に増やした効果は
第1段で 上位3頭に勝ち馬 65.4% → **67.3%**、logloss 1.864 → 1.855。
第2段の係数は `0.192 × log(基礎モデル) + 0.709 × log(人気)`。

回収率（大井297R＋川崎240R、1点100円、券種×BOX で計40通り）

| 券種・BOX | シミュ | ロジット | 合成 | 人気 |
|---|---|---|---|---|
| 馬連4頭（大井） | 63% | 61% | 70% | 72% |
| 三連複4頭（大井） | 63% | 64% | 83% | 88% |
| 馬連3頭（川崎） | 57% | **106%** | 78% | 77% |
| 三連複3頭（川崎） | 92% | **102%** | 85% | 83% |

**結論は変わらない。**
1. **ロジット ≫ シミュレータ**（上位3頭に勝ち馬 55% → 67%）。アプリの印はロジットに載せ替え済み
2. **合成は人気とほぼ互角**（大井 77.4% 対 77.1%、川崎 75.4% 対 75.8%）。超えてはいない
3. **回収率100%を安定して超える組み合わせはない。** 川崎の馬連3頭BOX 106% は
   240レースで的中35%、当たった数本が大きかっただけで、大井の同じ買い方は62%
4. 40通りのうち100%超えは3つだけで、すべて川崎の少点数BOX。**偶然の範囲**

### 特徴量を足した結果（29 → 39個）
| 追加したもの | 結果 |
|---|---|
| **テン・上がりのラップ** | **効いた。** `agariRel`（自身の上がり3F − そのレースの上がり3F）が −0.163 で全体6位。生の上がり3Fより明確に良い。`paceExp`（過去走のテンの速さ）も −0.099 |
| **南関のポイント制度** | **一部効いた。** `ptLog`（今回と前走の1着番組ポイントの比＝クラスの上下）が −0.087。一方 昇級初戦・降級初戦・連勝数・勝ち直後の据え置き はすべて係数ほぼ0で、**「ヤラズ」らしい痕跡は見つからなかった** |
| **同型馬の頭数** | **効かなかった。** 2通りのエンコード（自分を除いた前型比率／前型度×他の前型数）を試したが、`epos`（序盤の位置取り）が既に同じ情報を持っており、追加効果はほぼゼロ |
| **調教** | **不可**（上の表のとおり南関は非公開） |

検証945レースで logloss 1.873 → 1.864、上位3頭に勝ち馬 64.8% → 65.4%。伸びは小さい。

### 次にやるなら
- ロジットをアプリの印・勝率に載せ替える（3Dはロジットの確率に合うようノイズを較正して再生）
- 特徴量を足す：調教、乗り替わりの詳細、テン・上がりのラップ、同型馬の頭数
- 学習期間を伸ばす（いまは1年ぶん2,413レース。3年あれば係数が安定する）
- 締切1〜2分前のオッズを貯めて、最終オッズではなく実戦で使える値で検証し直す

---

## 予測モデル（`tools/fit_model.mjs`）

物理シミュレータは**順位予測には向いていない**（推定パラメータを真値として扱い、加える揺らぎが小さいので
勝率が1頭に集中する）。予測は条件付きロジットに分け、シミュレータは特徴量生成と3D再生に回す。

```
U_i = β・x_i                        馬ごとの強さ（単位は対数オッズ＝この案件の共通単位）
P(i が1着) = exp(U_i) / Σ_j exp(U_j)
```
1〜3着の並びまで使う Plackett–Luce の尤度を Adam で最大化する。依存ゼロ・Node標準のみ。

- **第1段**：オッズを使わない基礎モデル（`lib/feat.mjs` の29特徴量）
- **第2段**：`log p_基礎` と `log p_人気` をもう一度ロジットで合成。
  **第2段の第1項の係数が「人気に何を足せているか」の答え**。0に近ければ自前情報に価値がない

### 先読みを避ける
- 騎手・厩舎・コンビ・馬主・種牡馬の指数は `NK_DB_YEARS=2023,2024,2025` で作った
  `index.train.json` を使う（検証データの大半を占める2026年ぶんが母集団に入らない）
- 学習と検証は日付で切る（`NK_FIT_SPLIT`、既定 2026-06-01）
- 馬場・天候は当日発表の実際の値を使ってよい（レース前に分かる）
- **残っている先読み**：馬主の集計は cards.jsonl 全期間から作っているので検証期間も入る。重みは小さいが正確ではない

```bash
node tools/fetch_odds.mjs && node tools/fetch_results.mjs      # 学習データ（約80分・一度きり）
NK_DB_YEARS=2023,2024,2025 NK_DB_OUT=data/nankan/index.train.json node tools/build_db.mjs
NK_FIT_DB=data/nankan/index.train.json node tools/fit_model.mjs
NK_BT_TRACKS=大井,川崎 NK_BT_FROM=… NK_BT_TO=… node tools/backtest.mjs
node tools/embed_db.mjs
```

### オッズの取得
`/oddsJS/{raceId}.do` が1KB弱の JS で全頭ぶんの単勝・複勝と人気順を返す。
HTML のオッズページ（100KB）は使わない。発売前は `0.0` が並ぶので `update_time` と合わせて判別する。
オッズは発売中およそ1〜2分おきに更新される。

**締切前の自動取得（`tools/watch_odds.mjs`）**
南関（SPAT4）の発売締切は発走のおよそ1分前なので、`NK_ODDS_LEAD=8`（締切8分前）は
**発走の9分前**に取りに行く設定になる。同じレースについて締切前と最終の両方を残すので、
`odds_drift.mjs` で「8分前の数字で判断して間に合うか」を後から測れる。

さらに、あるレースの締切8分前を取ったタイミングで**まだ発走していない全レースの暫定オッズを取り直す**。
＝レースが1つ進むごとに後半のオッズが更新される。取るものがない時間帯も
`NK_ODDS_REFRESH`（既定12分）より古ければ取り直して鮮度を保つ。

- **翌日以降も対象**（`NK_ODDS_AHEAD` 既定3日）。南関は重賞などで前日発売があり、そこで値が入る
- **多重起動を防ぐロックが要る**（`.oddswatch.lock`）。launchd は1分おきに起動するので、
  1回の処理が1分を超えるとプロセスが重なり、サイトを叩きすぎて通信エラーの連鎖になる。
  9/8夜に実際これで 10R〜12R の締切前スナップショットを取り逃した
- 暫定オッズは**粘らない**（`get(..., { tries: 1 })`）。3回リトライすると1レース5秒かかり、
  肝心の締切前の取得まで遅れる。取れなければ次の周回で取り直せばよい
- 発売前・失敗は `.oddswatch.notyet.json` に覚えて `NK_ODDS_SKIP`（既定20分）あけてから再試行。
  翌日以降のレースは大半が発売前なので、これが無いと毎周回むだに叩く
- 1周回で取る暫定は `NK_ODDS_MAXPRE`（既定14）まで。発走が近い順に処理する
- 暫定は `odds_pre.json` に**1レース1件だけ上書き**する。毎分 jsonl に追記すると際限なく増える
- 締切前（`T-8`）と最終だけ `odds_live.jsonl` に追記し、あとで実際のズレを検証できるようにする

**発売中止（`sale:false`）の扱い**：オッズが `["0.0", false, …]` の馬は出走取消・除外。
出馬表への反映は遅れることがあり、**1頭でも欠けると `o.every(x => x > 0)` が false になって
レース丸ごと「オッズなし」に落ちていた**。`build_marks` / `build_results` で取消扱いにし、
頭数からも引く。画面には「発売中止」と出す。

**公開は `tools/publish.mjs` がまとめて行う（2026-09-17〜）。** 各反映ジョブ（refresh / refresh_boat / refresh_jra / refresh_banei / nightly_results）は
ローカルに commit するだけで、push は launchd `com.nankan.publish` が15分おきに「溜まっていれば」押す。Render は push ごとにデプロイするので、
1日600回 push していたら 9/16 17:27 でデプロイが止まった（パイプライン分数の枠）。これで多くても1日96回。`NK_PUSH_NOW=1` で従来どおりその場で push。
ログは `data/publish.log`。公開が古いと感じたら `git status -sb` の ahead と `data/publish.log` を見る。

**反映と公開（`tools/refresh.mjs`）**
オッズが変わったら `build_marks → build_top → embed_db` を回して各ページに埋め直し、
**git に commit / push して公開サイトへ反映する**（Render は main を見てデプロイするため、
push しないと手元のファイルが変わるだけで公開側は古いまま）。

- `odds_pre.json` / `odds_live.jsonl` の mtime+size で変化を見て、変わっていなければ何もしない
- `NK_MARK_FAST=1` で位置取り（3角・4角・ペース）の再計算を省くので **2秒**で終わる
  （省かないと48レース×600試行で2分かかり、実行間隔に収まらない）
- **commit するのは生成物だけ**（HTML 5枚と `data/nankan` の生成 JSON）。
  `tools/` や `*.md` は対象外なので、**作業中でも安全に走らせられる**
- `main` 以外のブランチでは push しない
- リモートが先行していたら fetch → rebase して押し直す（生成物は作り直せるので安全）
- 止めたいときは `NK_REFRESH_NOPUSH=1`

コミットは `chore(odds): 2026-9-8 19:15:49 時点のオッズを反映` の形で積む。
オッズ更新のたびに1コミットなので、開催日は1日あたり数十コミットになる。

```bash
sh tools/launchd/install.sh                # macOS に自動実行を登録（下の2つをまとめて）
#   com.nankan.oddswatch … 1分おきにオッズの取り時を見る
#   com.nankan.refresh   … 2分おきに、オッズが変われば各ページへ反映
node tools/watch_odds.mjs                  # 手で見張り続ける（開催終了で自動終了）
NK_ODDS_ONCE=1 node tools/watch_odds.mjs   # いま取り時のものだけ拾って終了
NK_REFRESH_FORCE=1 node tools/refresh.mjs  # 変化が無くても反映し直す
node tools/odds_drift.mjs                  # 締切前と最終のズレを見る
```

`build_marks.mjs` はオッズがあれば自動で第2段（人気との合成）まで使う。
締切前スナップショットを最優先し、無ければ最終オッズを使う。どちらを使ったかは
`entries.*.json` の `oddsSrc` と meta に残る。

**まだ検証していないこと**：締切8分前の値と最終オッズでどれだけ結果が変わるかは、
スナップショットが貯まるまで測れない。バックテストは最終オッズで回しているので、
実戦よりやや有利に出ている可能性がある。

### joint・段階モデル・正則化（2026-09-16、学習 7,931R／検証 1,017R、4場）
ボートで効いた3つを南関に移した。

| | logloss | 1着的中 | 上位3頭 | 3頭独占 |
|---|---|---|---|---|
| 第1段（オッズなし） | 1.862 | 35.4% | 66.7% | 9.3% |
| 単勝人気だけ | **1.639** | **42.5%** | 76.7% | 11.9% |
| 第2段 合成 | 1.661 | 41.6% | 76.0% | 11.9% |
| **joint（市場を特徴量に）** | 1.655 | 41.1% | **77.3%** | **12.1%** |

- **正則化（L2）は効かない。** 2／10／50 で 1.862 のまま、200 で 1.863、1,000 で 1.865、5,000 で 1.877、20,000 で 1.911 と強めるほど悪化。
  ボートと違って過学習していない（学習 1.784／検証 1.862 の差は情報量の差）。既定の 2.0 のまま
- **joint**：`fit_model.mjs` が `mktLog`（単勝オッズの対数確率、レース内で中心化）を特徴量に足して、オッズのあるレースで同時に当てはめる（`betaJ`、D+1 次元）。
  市場の係数 0.80、残った係数は **コンビ +0.14／馬主 +0.13／前走人気 +0.09／種牡馬の距離適性 +0.08／馬格 +0.08**＝市場が織り込み切れていないもの。
  logloss は第2段より良い（1.661 → 1.655）が**人気だけ（1.639）にはまだ届かない**。上位3頭・3頭独占は人気を上回る
- **2着・3着の段階モデル**（`stage`）：勝者を所与に「強さの差・序盤位置の差（epos）・枠の近さ・市場の差」で2着の5〜15択を別に学習。3着も同様。
  係数は 2着 `mktDiff` 0.75／`eposDiff` 0.18、3着 `eposDiff` 0.57／`mktDiff` 0.59（**序盤位置が近い馬が続けて来る**）。
  同じ勝率（joint）を土台にした公平な比較で 2着 logloss 1.857 → **1.844**、三連複 上位3点 29.3 → **30.2%**（本線 12.0% は同じ）。効きは控えめ
- 式は `lib/nkstage.mjs`（`predictRace` / `raceCombos` / `st2x` / `st3x`）に一元化。`build_marks` / `build_results` / `race_pick` / `backtest` は
  すべて `predictRace(MDL, f, tan, keepNos)` を通す（joint → 第2段 → 基礎 の順に使い、`src` に残す）。`lib/bets.mjs` の `betPlan` は第5引数 `combos` を受け取り、
  段階モデルの組の確率で期待値を出す（無ければ Harville）。**別式を作らない**
- 本番の印は `model.live.json`（全期間・split 2099）。`NK_FIT_SPLIT=2099-01-01 NK_FIT_DB=data/nankan/index.json NK_FIT_OUT=data/nankan/model.live.json node tools/fit_model.mjs`
- スイープは `NK_FIT_STAGE2=0 NK_FIT_JOINT=0 NK_FIT_STAGE=0` で第1段だけにすると1本1分
- backtest（2026-06-01〜09-16、最終オッズ、1点100円）で joint は合成・人気と同等かやや上：

| 買い方 | 大井 321R | 川崎 288R | 船橋 240R | 浦和 168R |
|---|---|---|---|---|
| 馬連4頭BOX joint／人気 | 82%／72% | 82%／74% | 71%／70% | — |
| 三連複4頭BOX joint／人気 | **91%**／89% | 80%／73% | 83%／67% | — |
| 三連複3頭BOX joint／人気 | 89%／66% | 89%／84% | 60%／64% | — |

  最終オッズなので実戦よりやや有利。締切前オッズの日（`odds_live.jsonl`）が貯まってから実運用の値で測り直す

### 買い方
BOX の均等買いは期待値に関係なく全組み合わせを同じ額で買うので構造的に不利。
`backtest.mjs` には期待値ベース（市場の `NK_BT_EDGE` 倍以上の確率の組み合わせだけ買う）も入れてある。
市場の組み合わせ確率は単勝オッズから Harville で推定する。

---

## データブラウザ（`data.html`）
数字だけを見たいとき用の別ページ。**index.html と同じく1ファイル・依存ゼロ**で、
`browse.json`（約770KB）を `NKBROWSE` マーカーに埋め込んである。

- タブ: 騎手／調教師／厩舎×騎手／馬主／種牡馬／母の父／場・距離の傾向／成績・回収率／指数の読み方
- 検索・最低出走数・場での絞り込み、列見出しクリックでソート、TSV コピー
- 行数が多いので既定200件ずつ表示（「さらに表示」で追加）
- 表示を足すときは `tools/build_browse.mjs` の `cols` と `data.html` の `TABS` を対で直す。
  コンビが7,600行あるため、行は配列で持ち騎手名・調教師名はインデックス参照にしている

---

## 傾向データ（`trend.<track>.json` / `meet.<track>.json`）
かつて「デモ用参考値（実集計に差し替え前提）」だった `TREND_REF` と、手入力だった `MEET` は**どちらも実測に置き換え済み**。

- `meet.<track>.json` … nankankeiba「レース傾向」ページの実測。開催日ごとの
  `front`/`back`（1〜3着馬の3角位置が頭数の半分より前／後ろだった回数）と `best`（3着内回数ベスト3枠）。
  1ページに開催の全日ぶんのブロックが入っているので、**日目でブロックを選ぶ**（URL は `…/race_trend/{年}{場}{回}{日}.do`）。
- `trend.<track>.json` … 出馬表の前5走欄 約5万走から距離別に集計。
  `front3`（3着内のうち3角で前方にいた割合）、`style`（3着内の脚質シェア）、
  `gate`（3着内の枠シェア）、`gateBase`（同じ枠の**出走**シェア）。

**枠の有利不利は `gate` だけを見ても分からない。** 枠ごとに入る頭数が違うので必ず
`gate ÷ gateBase`（1.00 が損得なし）で見る。実測では 川崎は内 1.14／外 0.90 で**内枠有利**、
大井はほぼ互角で 2000m だけ外有利。以前の文言（川崎は中〜外有利）は誤りだったので直してある。

`calib` は馬場バイアスのスライダーを自動算出するための基準値。
```
前残り度 = clamp((今回の前方3着内率 − frontMid) / (2.2 × frontSd), -1, 1)
内外     = clamp((中外−内の得失 − gateMid)     / (2.2 × gateSd),  -1, 1)
```
`frontMid` はその場のふつうの値、`frontSd` は開催日ごとのばらつき。
**「絶対値がいくつだから前残り」ではなく「その場のふつうと比べてどうか」で出す。**
場が変わっても意味が同じになるので、船橋・浦和を足しても係数を触らなくてよい。

---

## 出走馬の自動生成（`tools/build_races.mjs`）
出馬表の前5走から機械的に算出する。**手打ちしない。**

| 項目 | 算出 |
|------|------|
| `style` 脚質 | 前5走の**1角通過順 ÷ (頭数+1)** の加重平均。`≤0.22 または 絶対順位≤2.0`＝逃げ、`≤0.45`＝先行、`≤0.72`＝差し、以降＝追込。閾値は cards.jsonl 約12,800頭走の分布から 逃げ12%/先行33%/差し41%/追込14% になる位置 |
| `epos` | 上の比率そのもの。4分類より細かい序盤の位置取りとして `simRace` の `ke`・進路に効かせる |
| `ability` | `0.19 + 0.58 × (着順の相対順位の加重平均)^1.25`。連勝級≒0.73、掲示板≒0.58、大敗≒0.25 |
| `close` | 上がり3F から `0.70 − (平均3F − 36.5)×0.10`。上がり順位で1/4だけ補正 |
| `stamina` | 今回距離±200m の実績。未経験の距離延長は低め |
| `wet` | 良以外（稍重・重・不良）での相対着順 |
| `note` | 寸評。`[見出し, 本文]` の配列で【近走】【脚質】【条件】【人】【血統】の5本立て。**1項目1行に収める**（文章にすると読まれない）。例：`前走 川崎1500 4人気後方4着／近5走 4-2-4-5-3（3着内2）` |
| `brief` | 縦型の柱に入れる1行。目を引く要素だけ（`前走2人気2着・先行・スポット`） |
| `memo` | note のうち【近走】【人】だけを連ねた短い版（index.html 用） |

重みは前走から `[1, .85, .7, .55, .45]`。出走取消馬は除外（頭数 `n` と配列長は自動で一致する）。

---

## index.html のセクション
| # | 内容 | 触る頻度 |
|---|------|---------|
| 0 | ユーティリティ（RNG, clamp, $） | 低 |
| 1 | データ: `STYLE` 脚質係数, `BABA`, `WEATHER`, `REF_TIME`、**`NKDATA`（生成・手を入れない）** と そこから組み立てる `TREND_REF` / `MEET` / `calib` | 高 |
| 1b | `TRACKS` 開催場の設定 | 中 |
| 2 | コース幾何 | 低 |
| 3 | サンプル出走馬生成 `genHorses`（実データが無いレース用） | 低 |
| 4 | シミュレーション `humanEff` / `simRace` / `monteCarlo` / `paceOf` | 中 |
| 5 | 状態 `state`, `combinedTrend`, `gateEdge`, `autoBias`, `loadRace` | 中 |
| 6 | 3D 描画 `scene3` | 低 |
| 7 | HUD | 低 |
| 8 | 右パネル `renderPred` / `renderHuman` / `renderPoints` / `trendPoint1` / `trendPoint2` / `renderTrend` | 中 |
| 9 | UI 配線 `init` | 低 |

### 開催場の切替（セクション 1b `TRACKS`）
`TRACKS.oi` / `TRACKS.kawasaki` に `geo`（コース幾何・ゴール位置・左右回り mirror）、`cp`（3角/4角の残距離）、
`lanePh`、`ref`（距離別基準タイム）、`trendRef`、`meet`（実測傾向）、`days`／`real`（★生成データ）、
`styleAdj`、`defaults`、`scene`、`text` をまとめている。起動時に `?track=` から `TK` を決める。切替は再読込。

**船橋・浦和を足すとき**（データ側はもう揃っている）:
1. `REF_TIME_FB`（距離別基準タイム）を作る。船橋＝左回り1周1,400m・直線308m、浦和＝左回り1周1,200m・直線220m
2. `NK_RACE_TRACKS='大井:oi,川崎:kawasaki,船橋:funabashi,浦和:urawa'` で
   `fetch_cards → fetch_trend → build_trend → build_races → embed_db` を回す
3. `TRACKS` に1エントリ追加（`trendRef` と `meet` と `calib` は生成データから自動で入る）。
   人的要因の指数は最初から4場ぶん取ってあるのでそのまま効く

### 読みのポイントの文言
`TK.text.point1 / point2` は `trendPoint1 / trendPoint2` を呼ぶだけにしてある。
**場ごとの数字を文章に手で書かない。** データが変われば文章も変わるようにしておく。

---

## シミュレーションを調整するとき
- 脚質の前半/後半の強さは `STYLE.ke`（序盤）/ `STYLE.kl`（終盤）。合計スプレッドを大きくすると前残り一辺倒になる
- 前残り度の効き: `simRace` 内の `0.045*fb`、外伸びの効き: `0.03*ob*laneF`
- 人的要因の効き: 上の `hx*...` 4か所
- ペース: `paceOf`。逃げ2頭以上で H、`pf` が前の失速と差しの伸びを動かす
- 馬場: `BABA[].speed`（時計）, `.front`（前残り上乗せ）, `.noise`（ばらつき）
- **変更後は必ず `node tools/sim_check.mjs` を回す。** 見る点は
  (a) 逃げの3角平均が1〜2番手台に収まっているか
  (b) 差し馬場（front −0.5）で差しの勝率が前残り時の5倍以上に伸びるか
  (c) 人0/1/2 の列で人的要因が馬の力を食っていないか

---

## JRA（中央競馬）版 — 2026-09-10 着手

南関と同じ構成で作る（データ層 `data/jra/` ＋ ツール層 `tools/jra_*.mjs` ＋ 1ファイルのページ）。南関のコードとは混ぜない。
指数の単位（対数オッズ差）、縮小推定、先読み回避、「モデルは1つ・検証も同じ式」の約束は南関・ボートと同じ。

### データ元（netkeiba のレースページ。無料・UTF-8・JavaScript 不要）
| 用途 | URL | 備考 |
|---|---|---|
| 開催日 | `race.netkeiba.com/top/calendar.html?year=&month=` | `kaisai_date=YYYYMMDD` を拾う |
| レース一覧 | `race.netkeiba.com/top/race_list_sub.html?kaisai_date=` | `race_id`（12桁：年4 場2 回2 日2 R2） |
| **馬柱（前5走つき）** | `race.netkeiba.com/race/shutuba_past.html?race_id=` | 父・母・母父・厩舎・脚質・間隔・騎手・斤量、前5走（場・距離・タイム・馬場・頭数・人気・通過順・上がり・馬体重・勝ち馬と着差） |
| **結果・払戻** | `race.netkeiba.com/race/result.html?race_id=` | 着順・タイム・着差・人気・単勝オッズ・上がり3F・通過順・厩舎・馬体重、全券種の払戻と人気、コーナー通過順、200mラップとペース(S/M/H) |
| オッズ(JSON) | `race.netkeiba.com/api/api_get_jra_odds.html?race_id=&type=1&action=init` | 発売前は `status:"yoso"`（予想オッズ）。Referer 必須 |

場コード `01`札幌 `02`函館 `03`福島 `04`新潟 `05`東京 `06`中山 `07`中京 `08`京都 `09`阪神 `10`小倉。
2023年9月の結果ページも同じ構造で取れることを確認済み（学習は3年ぶん・約1万レース）。

**netkeiba は 1.2秒間隔で約4,000ページ取ったところで規制に入った**（race.netkeiba.com 全体が HTTP 400、UA を変えても同じ、
db/sp のサブドメインも同じ＝IP 単位）。そこで**既定のデータ元は Yahoo!スポーツ競馬**（`lib/yahoo.mjs`、`JRA_SRC=yahoo`）。
馬ID・騎手ID・調教師IDは netkeiba と同じ体系なので、netkeiba で取った3,916レースと混ぜて使える。

| 用途 | Yahoo の URL | 備考 |
|---|---|---|
| 月間日程 | `sports.yahoo.co.jp/keiba/schedule/monthly?year=&month=` | 開催ID（yy 場2 回2 日2） |
| レース一覧 | `/keiba/race/list/{開催ID}` | 日付・レース名・race_id（10桁＝12桁の先頭 "20" を除いたもの） |
| **結果・払戻** | `/keiba/race/result/{race_id10}` | netkeiba と同じ項目（馬体重は結果表の中）。ラップは「累計(ラップ)」の括弧内。ペースの記号は無い |
| 出馬表 | `/keiba/race/denma/{race_id10}` | 枠・馬番・性齢毛色・騎手・斤量・調教師(所属)・父母母父・馬体重・人気オッズ。**前5走は無い**→ `results.jsonl` から馬IDで組む |

`lib/jra.mjs` の `get()` は 400/403/429 が来たら10分休み、6回続いたら中断する。間隔の既定は3秒（Yahoo は 2.5秒で運用）。
netkeiba の馬柱（`shutuba_past`）は規制が解けたときだけ使う（脚質・間隔の表示があるので出馬表としては上）。

**取れないもの**：`db.netkeiba.com` のリーディング（空応答）→ 騎手・調教師・種牡馬の指数は結果から自前で作る。
追い切り（`oikiri.html`）は JavaScript 描画で本文が無い → 調教は使えない（南関と同じ）。馬主は馬柱に出ない。
枠順は木〜金に確定するので、それより前の馬柱は枠・馬番が空。**枠順確定前は除外対象馬も並ぶ**（馬IDのリンクが無い行）ので落とす。

**マナー**：`lib/jra.mjs` が 1.2秒間隔・単一スレッド・キャッシュ（`data/cache/jra/`）。並列取得はしない。
直近4日のページは 30分で取り直す（結果が出る前の空ページを抱えない）。

### ファイル
```
jra.html                 出馬表（新聞配色の馬柱）＋予想。NKJRA マーカーに races.json を埋める。スマホは1頭1セルで前5走を横スクロール
data/jra/results.jsonl   結果（.gitignore。1レース1行。キャッシュから作り直せる）
data/jra/cards.jsonl     出馬表（.gitignore。前5走は results から馬IDで組んだもの）
data/jra/index.json      ★騎手・調教師・コンビ・コース・馬の指数 — コミット対象
data/jra/index.train.json ★先読みを避けた学習用の指数（JRA_DB_TO で切る）
data/jra/model.json      ★条件付きロジットの係数・温度・第2段・検証結果 — コミット対象
data/jra/backtest.json   ★買い方ごとの的中率・回収率 — コミット対象
data/jra/races.json      ★今日以降の出馬表と予測（jra.html 用）／top.json（TOP 用）— コミット対象
tools/lib/jra.mjs        取得共通（レート制限・キャッシュ・規制時の休止・jsonl の差し替え追記）
tools/lib/jrapage.mjs    netkeiba のパーサ（規制が解けたときだけ）
tools/lib/yahoo.mjs      Yahoo!スポーツのパーサ（parseMonthly / parseList / parseResult / parseDenma）
tools/lib/jfeat.mjs      特徴量 58個（南関の feat.mjs を踏襲。学習も予測も raceFromResult / raceFromCard で同じ形にしてから featurize。レースレベル・基準タイム・市場の対数確率もここ）
tools/lib/jbets.mjs      組の確率（馬連・馬単・三連複・三連単・ワイド。3着までの並びを全列挙）
tools/jra_fetch_results.mjs  結果を取る（JRA_FROM/JRA_TO、JRA_SRC=yahoo|netkeiba、取得済みは飛ばす）
tools/jra_fetch_cards.mjs    出馬表を取る（今日〜3日後）
tools/jra_build_db.mjs       指数 → index.json（JRA_DB_FROM/TO/OUT）
tools/jra_fit.mjs            当てはめ → model.json（JRA_FIT_SPLIT/WARM/DB/EPOCH/L2）
tools/jra_backtest.mjs       検証 → backtest.json（JRA_BT_LEVEL=base|mix）
tools/jra_build_races.mjs    出馬表に当てる → races.json / top.json
tools/jra_check.mjs          DOM スタブで jra.html の全レースを描画して検査
tools/refresh_jra.mjs        反映係（launchd: com.jra.refresh、20分おき）
```

### パイプライン
```bash
JRA_FROM=20230901 JRA_TO=20260910 JRA_WAIT=2500 node tools/jra_fetch_results.mjs   # 3年ぶん。約1万レース・6時間（取得済みは飛ばす）
node tools/jra_fetch_cards.mjs                                                      # 今日〜3日後の出馬表
node tools/jra_build_db.mjs                                                         # 指数
JRA_DB_TO=2026-05-31 JRA_DB_OUT=data/jra/index.train.json node tools/jra_build_db.mjs   # 学習用（先読み回避）
JRA_FIT_DB=data/jra/index.train.json node --max-old-space-size=4000 tools/jra_fit.mjs   # 当てはめ（学習 2024-03〜2026-05／検証 2026-06〜）
node --max-old-space-size=4000 tools/jra_backtest.mjs                               # 検証
node --max-old-space-size=4000 tools/jra_build_races.mjs && NK_EMBED_ONLY=jra node tools/embed_db.mjs && node tools/jra_check.mjs
```

### 学習の作り（南関との違い）
- 学習用のレースは results そのもの（騎手・斤量・枠・馬体重はレース前情報）。前5走は馬IDで「その日より前」の結果から引く。
  データの先頭6か月は前走が揃わないので `JRA_FIT_WARM`（既定 2024-03-01）より前は学習に使わない
- 障害戦は除く。取消・除外・中止は履歴に入れない
- 温度（τ1/τ2）は学習の末尾2割で決める（全体で決めると β の過学習ぶんまで鋭くなり、検証で強気に出た）
- 第2段は単勝オッズ（results は確定オッズ＝実戦よりやや有利）。出馬表側のオッズは Yahoo の denma が発売後に持つ
- 枠順確定前（木〜金）の出馬表は馬番が無い。その間は順位と確率だけ出し、BOX・組の確率は出さない（`gates:false`）

### 人的要因は「レース時点」の指数で（`buildAsOf`）— **必ず守る**
期間まとめの `index.train.json` を学習に使ったら、検証（2026-06〜09）で 1着的中 23.6%（人気だけの 35.3% より大きく下）になった。
期間ごとに「勝ち馬の特徴量 − レース平均」を比べると、**学習期間ではコンビ指数・コース枠バイアス・騎手の場別が勝ち馬を強く
言い当てているのに、検証期間ではほぼ 0**。学習中は自分の結果を含んだ指数を見ている（先読み）ためで、
JRA はコンビ・枠の標本が南関より小さいので影響が大きい。
対処：`lib/jfeat.mjs` の `buildAsOf(results)` が結果を日付順に流し、各レースについて「その日より前の結果だけ」で
騎手／調教師／コンビ／騎手の場別・芝ダ別／調教師の芝ダ別／騎手の直近60騎乗の調子 を縮小ロジットで作る。
出馬表には最終状態を使う。コース枠バイアス（`gateEdge`）は外した。`index.json` は表示（根拠の文言）と参照用。

### 検証結果（2026-09-12、学習 2024-03〜2026-05 の 7,516R／検証 2026-06〜09 の 958R）

| | 基礎モデル | 1番人気だけ | 第2段（合成） |
|---|---|---|---|
| logloss | 2.135 | **1.881** | 1.880 |
| 1着的中 | 27.4% | **35.3%** | 35.3% |
| 上位3頭に勝ち馬 | 58.9% | **67.8%** | 67.7% |

- **人気に届いていない。** 第2段の係数は 0×基礎 + 1.05×人気で、基礎モデルは市場に何も足せていない
- 較正は改善（予測45%→実際34%、以前は 45%→28%）。温度 τ1 1.15／τ2 1.01
- 回収率（検証 958R・合成）：◎単勝 82.1%（1番人気の単勝 81.2%）、◎複勝 84.4%、3頭BOX三連複 82%。100% 超えなし
- 次に効きそうなもの：コース別の騎手成績の細分化、前走のレースレベル（勝ち馬のその後）、調教（取れない）

### 馬体重・脚質・毛色（データ値として出す）
- `jra_build_db` のコース（場×芝ダ×距離）に **出走平均・3着内平均・勝ち馬平均の馬体重**と `bwDiff3`（3着内 − 出走平均。＋なら大型有利）、
  **脚質別の出走数・3着内率・シェア**（序盤位置 逃げ≤20%・先行≤42%・差し≤70%・追込）を持つ。全体の馬体重帯別成績は `bwBands`
  （440kg未満は勝率5%以下、500kg台が最も高い）
- 馬ごとに `derive()` が **好走時（3着内）の平均馬体重・普段の平均と幅・近走の脚質の内訳**を出す。馬柱の体重の横に「好走480」、
  好走時から ±12kg 以上離れていれば赤字。根拠の【馬体】と【脚質／毛色】、読みのポイントに「このコースは大型有利 +6kg」などを出す
- 特徴量：`bwFit`（好走時との差、3着内2走以上の馬だけ）、`bwEdge`（コースの大型有利度 × 出走平均との差）。効き目は次の当てはめで測る
- 毛色は表示のみ（予測には使わない）

### 血統・馬主（`jra_fetch_horses.mjs` → `data/jra/horses.jsonl`、2026-09-12 に取得開始）
Yahoo の馬ページ `/keiba/directory/horse/{id}/` に 父・母・母父（血統表の gen1st / genSecond の3番目）・馬主・生産者・産地がある。
学習期間に出走した約19,000頭を 3.5秒間隔で取る（約18時間・一度きり。血統は変わらないので 3650日キャッシュ）。
出馬表に出た新しい馬は `refresh_jra` が毎回数頭ずつ取り足す。
特徴量は `buildAsOf` が種牡馬・母父・馬主の勝率を「レース時点」で縮小ロジットにしたもの：
`sIdx`（k=80）・`bmsIdx`（k=80）・`sSurf`（芝ダ別の偏り）は南関と同じく **前走が少ない馬ほど効かせる**（×max(0, 1−前走数/4)）、
`oIdx`（馬主 k=40）は常時。

**結果（2026-09-13 当てはめ直し、学習 7,516R／検証 1,004R）**：係数は sIdx 0.59／bmsIdx 0.40／sSurf 0.29／oIdx 0.14／bwFit 0.07／bwEdge 0。
基礎モデル logloss 2.135 → **2.122**、1着的中 27.4% → 27.2%、上位3頭 58.9% → 59.1%。**血統は効くが小さく、人気（35.6%）には遠い。**
第2段の係数は依然 0×基礎 + 1.05×人気（市場に足せていない）。

### 市場を特徴量に入れて同時に当てはめる `joint`（2026-09-14）
第2段（基礎モデルと人気を後から混ぜる）は係数が 0×基礎 になり、「基礎モデル丸ごと」では市場に何も足せなかった。
そこで `mktLog`（単勝オッズの対数確率、レース内で中心化）を特徴量の1つにして**他の全部と同時に**当てはめる（`jra_fit.mjs` の joint）。
特徴量ごとに「市場の上に足せる分」だけが係数に残るので、第2段より細かい。
- `base`（オッズなし）は `mktLog` の係数を 0 に固定して当てはめる。`model.json` には base と joint の両方が入り、
  `jra_build_races` / `jra_backtest` は**オッズの出たレースだけ joint**（level `joint`）、それ以外は base を使う
- 結果（検証 1,004R）：joint logloss **1.878**、人気だけ 1.8782、第2段 1.8771。1着的中 35.2%（人気 35.6%）、上位3頭 68.9%（68.1%）。
  **市場と互角止まり。** 残った係数は `ability` −0.45（市場は近走の着順を買いかぶる＝逆張り）、`nRuns` +0.37、`epos` −0.31、`spdBest` −0.20 など
- 学習側のオッズは確定値なので実戦（締切前）よりやや有利。joint の数字は上限と見る

### レースレベル（相手関係）— 2026-09-14
「前走○着」の意味はレースの質で変わる。各レースについて**出走馬が次走でどう走ったか**（相対着順の平均の 0.5 からのズレ、k=6 で縮小）を
レベルにする（`buildRaceIndex` の `levelOf(raceId, before)`）。**今回のレース日より前に走った次走だけ**で集計するので先読みは無い。
180日以上あいた次走は数えない。レベルは −0.5〜+0.5、実際の分布は 平均 0.01・標準偏差 0.06（画面では ×100 で `Lv+12` のように出す）。
特徴量：`raceLvl`（前5走のレベルの加重平均）／`lvlX`（(相対着順−0.5)×レベル＝強い相手で好走したか）／`lastLvl`（前走のレベル）。
**効いた**：base の logloss 2.1186 → **2.1049**、1着的中 26.9% → 27.5%、上位3頭 59.5% → 60.6%。`raceLvl` の係数 0.644 で全体4位。
joint も 1.878 → **1.8747** で、**初めて人気だけ（1.8782）を下回った**（差は 0.0035、検証 1,004R なので誤差の範囲を出ない）。
表示：馬柱の前走コマに `Lv+12`（+6 以上は赤、−6 以下は灰）、根拠に【相手関係】、読みのポイントに
「前走のレベルが高い」「上位人気だが前走の相手は弱かった」を出す。

### 速度指数の作り直し（2026-09-14）
以前の `spdIdx` は「コース平均の勝ち時計との差＋固定の馬場補正」だった。`buildRaceIndex` が結果から
**200m あたりの基準タイム＝コース平均＋馬場状態の補正（芝ダ別）＋クラスの補正（芝ダ別）**を残差の平均で2回まわして求め
（`RI.stdTime(venue, surface, dist, baba, cls)`、補正は k=30 で縮小）、さらに**ペース補正**（そのレースのテン3F がコース平均より
遅ければ、全体の時計が遅くなるぶんの半分を戻す）を掛ける。求まった補正：芝の重・不良は +0.15秒/200m、**ダートの重・不良は −0.13〜−0.16（速くなる）**、
クラスは 未勝利 +0.10 → OP −0.10〜−0.21 と素直。
**効いた**：base の logloss 2.1049 → **2.0991**、上位3頭 60.6% → 60.7%（1着的中 27.1%）。`spdBest` の係数 0.894 で全体2位。
joint は 1.8747 → 1.8744 で変わらず（市場は時計を織り込み済み）。
基準は全期間の結果から作っているので、厳密には検証期間の時計が基準に入っている（平均値なので影響は小さい）。

### 検証のまとめ（2026-09-14 時点、学習 7,516R／検証 1,004R）
| | base logloss | 1着的中 | 上位3頭 | joint logloss |
|---|---|---|---|---|
| 血統・馬主まで | 2.1186 | 26.9% | 59.5% | 1.8780 |
| ＋レースレベル | 2.1049 | 27.5% | 60.6% | 1.8747 |
| ＋速度指数の作り直し | 2.0991 | 27.1% | **60.7%** | 1.8744 |
| ＋学習5年（2021-07〜、16,412R。血統は3年ぶんのまま） | 2.0945 | 26.6% | 59.8% | 1.8736 |
| ＋2021〜2023 の血統も取得（31,701頭、2026-09-16） | **2.0946** | 27.5% | **60.1%** | **1.8712** |
| 人気だけ | 1.8782 | 35.6% | 68.1% | — |
base は人気にまだ遠い（logloss で 0.22）。joint は人気を 0.004 下回るだけで、回収率（◎単勝 81%前後）は 1番人気買いと同じ水準。
**JRA は市場が強く、いまの材料では上乗せできていない。** 学習を5年（2021-01〜、2026-09-15 取得完了・18,164R）に伸ばしても logloss で 0.005 しか動かない。
2021〜2023 に出走した馬の血統（12,549頭）は 2026-09-15 に取得し、9/16 に当てはめ直した：基礎は変わらず（2.0946）、joint が 1.8736 → 1.8712 とわずかに改善。**血統を5年ぶん揃えても市場との差は 0.007**。JRA は特徴量の追加では動かない段階に来ている。

### 日別・場別の成績（`jra_build_results.mjs` → `results.json` → TOP の中央競馬面）
ボートと同じ仕組み。`jra_build_races` が**発走の過ぎたレース**の予想（順位・1着確率・AIの上位N点・1番人気）を `preds.jsonl` に記録し、
結果と突き合わせる。`JRA_RECORD_PAST=1` は記録の仕組みができる前の日を「再現」（late=9999）として後から入れる用（2026-09-12/13 に使った）。
第2段の係数が 0×基礎 のうちは **◎＝1番人気**なので、◎単勝の回収率は「1番人気の単勝」と一致する（そういう状態だと分かるように基準列を並べる）。

---

## ばんえい競馬版（帯広）— 2026-09-14 着手

南関・JRA と同じ構成（データ層 `data/banei/` ＋ ツール層 `tools/banei_*.mjs` ＋ 1ファイルの `banei.html`、TOP のタブ）。他と混ぜない。
ばんえいは直線200mの1コースだけ、10頭立て、着順は **積載重量・馬体重・馬場水分（含水率）** に強く左右される。
含水率が高いほど砂が締まって速く（水分 0.8% で勝ち時計 2分超、3% で 1分40秒台）、乾くと重い。

### データ元（地方競馬情報サイト keiba.go.jp、NAR 公式・UTF-8）
| 用途 | URL | 備考 |
|---|---|---|
| 月別開催日程 | `/KeibaWeb/MonthlyConveneInfo/MonthlyConveneInfoTop?k_year=&k_month=` | 「帯広ば」の行の RaceList リンクから開催日 |
| 当日メニュー | `/KeibaWeb/TodayRaceInfo/RaceList?k_raceDate=YYYY%2FMM%2FDD&k_babaCode=3` | 発走・競走名・天候・**馬場水分**・頭数・変更情報（出走取消） |
| 出馬表 | `…/DebaTable?k_raceDate=&k_raceNo=&k_babaCode=3` | 1頭＝5行。馬体重（当日）・オッズ・積載・減量記号（☆＝10kg／△＝20kg）・父母母父・馬主・生産牧場・着別成績・**前5走（着順・日付・馬場水分・頭数・馬番・競走名・人気・馬体重・騎手・積載・タイム・着差）** |
| 競走成績 | `…/RaceMarkTable?…` | 着順・枠・馬番・性齢・積載・騎手・調教師・馬体重(増減)・タイム・人気・単勝オッズ・**払戻（全券種）**。優勝馬の血統は 2026-04 以降だけ |
`babaCode=3` が帯広。**単勝オッズの列は最近のページにしか無い**（2025年以前は人気だけ）。市場の見立てが要るところは
「人気順位ごとの勝率」（`lib/bnfeat.mjs` の `marketProbs`）で代用する。同じ枠の2頭目は枠のセル（rowspan）が無い（`parseCard` で引き継ぐ）。
払戻は `<section class="newRefundTable">`（ページ先頭の「当日払戻金」リンクを拾わないこと）。

**マナー**：`lib/bn.mjs` が 2秒間隔・単一スレッド・キャッシュ（`data/cache/banei/`）・ネット切断は待つ。1日の取得は 当日メニュー1＋出馬表12＋成績12。

### ファイル
```
banei.html                 出馬表（新聞配色の馬柱：積載・馬体重・水分つき前5走）＋予想＋水分帯・コース別の実測。NKBANEI マーカー
data/banei/results.jsonl   成績（.gitignore。1レース1行）／cards.jsonl 出馬表（.gitignore。血統・馬主・前5走の元）
data/banei/index.json      ★騎手・調教師・コンビ・種牡馬・母父・馬主・馬番・水分帯・馬体重帯の集計
data/banei/model.json      ★条件付きロジット（base・joint）／backtest.json／races.json／top.json／preds.jsonl／results.json
tools/lib/bn.mjs           取得共通　tools/lib/bnpage.mjs パーサ（parseMonthly / parseRaceList / parseResult / parseCard）
tools/lib/bnfeat.mjs       特徴量 44個（下）　tools/banei_fetch.mjs 取り込み（BN_FROM/TO、BN_KIND=results|cards|both）
tools/banei_build_db.mjs   index.json　banei_fit.mjs 当てはめ　banei_backtest.mjs 検証　banei_build_races.mjs 出馬表に当てる
tools/banei_build_results.mjs 日別成績　banei_check.mjs DOM スタブ検査　refresh_banei.mjs 反映係（launchd: com.banei.refresh、15分おき）
```

### 特徴量（`lib/bnfeat.mjs`）— 南関の項目＋ばんえい固有
- 近走：相対着順の加重平均、クラス差込み、前走の着順・勝ち馬との秒差、走数、連勝、勝った直後
- **時計**：`stdTime(水分, クラス)`＝水分ビン（0.5%刻み）の平均勝ち時計＋クラス補正（k=20）との対数差。水分で時計がまるごと変わるので生の時計は使わない
- **積載**：レース平均との差（`loadRel`）、前走からの増減（`loadChg`）、**馬体重に対する比**（`loadBw`）
- 馬体重：対数・レース内相対・増減・普段との差・増減の絶対値（800〜1,200kg）
- **馬場水分**：`moistX`＝馬ごとの「水分2.0以上と1.2以下での相対着順の差」×今日の水分の偏り、経験した水分の平均との差、前走との差
- **コース**：馬番（10コース）と、馬番別の「3着内シェア÷出走シェア」（`index.json` の `gate`）
- 人：騎手・調教師・コンビ・騎手の直近60騎乗（レース時点、`buildAsOf`）、減量騎手
- 血統・馬主：種牡馬・母父（前走が少ない馬ほど）・馬主（レース時点）。血統は出馬表から（`loadPed`）
- クラスは競走名の末尾から：2歳・3歳は D→C→B→A→OP、3歳以上は C2→C1→B4→B3→B2→B1→A2→A1→OP（`classOf`）

### 検証結果（2026-09-15、成績 5,323R＝2023-09〜2026-09-13。学習 2024-03〜2026-03 の 3,543R／検証 2026-04〜09 の 790R）
| | logloss | 1着的中 | 上位3頭に勝ち馬 |
|---|---|---|---|
| base（市場なし） | 1.952 | 28.7% | 60.6% |
| 人気だけ（人気順位ごとの勝率） | **1.725** | **37.2%** | **69.9%** |
| joint（市場＋全特徴量） | 1.744 | 36.8% | 69.1% |
- 係数の上位：`clsAbility` +3.8／`ability` −3.3（相殺しあっている＝クラス込みの相対着順が効く）、`bwLog` +1.3（**大型馬が有利**）、
  `loadBw` −0.9（**馬体重に対する積載が重いほど不利**）、`spdIdx` +0.7（水分補正の時計）、`gateEdge` +0.5（馬番の得失）、`layoff` −0.5、`mare` −0.3
- 回収率（検証 790R、joint、1点100円）：◎単勝 84.9%（1番人気の単勝 80.6%）、◎複勝 91.3%、3頭BOX三連複 87.4%、AI 三連単 上位3点 92.3%。100% 超えなし
- **市場（人気順位）に届いていない。** joint が人気だけを下回る。L2 を 1e-3 → 1e-2 にしても joint 1.7433（変わらず）。
  「人気順位ごとの勝率」は同じデータで作った較正済みの見立てなので、これを下回らないだけでも難しい。次は特徴量の整理（`ability` と `clsAbility` の相殺）と、
  積載×水分の交互作用
- 単勝オッズは 2026-02 以降の 987R にしか無い。それ以前の「人気」は確定オッズの順位なので、市場の見立てとしては実戦よりやや有利
- 馬場水分別の実測：勝ち時計は 〜0.9% で 121秒、4%〜 で 102秒。1番人気の勝率は乾いた馬場 39%、水分が多いと 25〜33%（**水分が多いと荒れる**）
- 馬番の得失：2・3・7番がやや良く（1.03〜1.06）、10番が 0.91

### ばんえい特化の特徴量（2026-09-18、こたの実感を数字にしたもの）
「その開催を通じて同じ馬番が好走する」「外枠が来る開催は続く」（直線200mの10コース＝砂圧など数字にならないものが結果に出る）を、
**そのレースより前の結果だけ**から積む（`lib/bnfeat.mjs` の `buildLaneIndex`。開催＝日付の差が1日以内でつながる連続開催日）。
| 特徴量 | 中身 | 係数（base） |
|---|---|---|
| `laneMeet` | この開催ここまでの、その馬番の 3着内回数 − 期待（k=6） | **+0.20** |
| `laneDay` | 今日ここまでの同じもの（k=3） | −0.05（短い傾向は効かない） |
| `outerMeet` / `outerDay` | 外（8〜10）− 内（1〜3）の3着内率 × 自分の内外 | +0.07／−0.02 |
| `loadMoist` | 積載比（馬体重に対する積載）× 水分 | **+0.39（全体11位）** |
| `loadTop` | トップハンデ（レース最大の積載、2頭まで） | +0.16 |
| `jMoist` | 騎手の「今日の水分帯（2.0以上／1.2以下）」での上振れ（レース時点） | +0.07 |
| `tForm` | 厩舎の直近60走の調子（レース時点） | +0.07 |
| `bwMoist` | 馬体重の相対 × 水分 | +0.02 |
結果（同じ分割、検証 802R）：base logloss 1.948 → **1.945**、上位3頭 61.1 → **62.1%**、joint 1.739 → **1.737**。
**開催を通じた馬番の傾向は実際に効く。当日だけの短い傾向は効かない。** 積載×水分の交互作用も効いた。
- `refresh_banei` は開催中（10〜21時）に今日の結果を毎回取り直す（終わったレースだけ増える）ので、`laneDay / laneMeet` が当日の途中から入る
- 画面：読みのポイントに「開催中のコース傾向：本日ここまで nR は 外（8〜10番）が来ている（外−内 +0.26）。この開催（9/12・9/13〜31R）は…好走が続く馬番：9番（+0.15）」、
  根拠の各馬に「n番コースの開催中の傾向：今日ここまで／この開催」、根拠のグループに「当日コース」
- backtest（joint、802R）：◎単勝 84.2%・◎複勝 91.5%・3頭BOX三連複 95.3%（前 87.4%）・AI三連単上位3点 80.8%（前 92.3%）。検証集合が少し違い、1本の高配当で動く幅

## ボートレース版

競馬側とは別の競技だが、**方針は同じ**（1ファイル・依存ゼロで配る／データは `data/` + `tools/` に分ける／
指数の単位は対数オッズ差にそろえる／推定値には出典を添える）。

### なぜボートは競馬と別のモデルなのか
着順が**進入コースにほぼ支配される**（1コースの1着率は全国で50〜62%、6コースは2〜3%）。
素の勝率をそのまま実力として扱うと、1号艇に乗る機会の多い選手が全員名人に見える。
指数は必ず **「場×コースの基準勝率からの上振れ」** で持つこと。

### データ元（2系統）

**① 公式ダウンロード（過去の蓄積・学習用）** — `https://www1.mbrace.or.jp/od2/{K|B}/YYYYMM/{k|b}YYMMDD.lzh`

- **1ファイルにその日開催した全場が入る**。`24KBGN 〜 24KEND` の形で場コードのブロックが並ぶ。
  場ごとに取りに行く必要がない（1日2リクエストで全24場）
- LHA(lh5) だが **macOS 標準の `bsdtar -xf` で展開できる**。lha/7z/unar を入れない（依存ゼロを維持）
- Shift_JIS。**固定長だが桁は「Shift_JIS のバイト位置」で決まる**ので、
  デコード後の JS 文字列を `slice` するとズレる。必ず `tools/lib/od2.mjs` の `bslice()` を使う
- **2010年9月まで遡って取れる**ことを確認済み（それ以前は未確認）
- K（競走成績）… 全券種の払戻金、天候・風向・風速・波高、着順・モーター・ボート・**展示タイム・進入コース・ST**・
  レースタイム・決まり手
- B（番組表）… 級別・全国勝率/2連率・**当地勝率/2連率**・モーターNo/2率・ボートNo/2率・今節成績・
  年齢・支部・体重・電話投票締切予定時刻

**② 公式Web（当日の直前情報）** — `https://www.boatrace.jp/owpc/pc/race/<page>?rno=R&jcd=場&hd=YYYYMMDD`

| ページ | 取れるもの |
|---|---|
| `beforeinfo` | **展示タイム・チルト・プロペラ新・部品交換・調整重量・スタート展示（進入コースと展示ST）・水面気象（気温/天候/風速/風向/水温/波高＋計測時刻）** |
| `oddstf` | 単勝・複勝オッズ、各レースの締切予定時刻 |
| `odds3t` / `odds3f` | **3連単120通り／3連複20通りのオッズ**。南関のように Harville で推定しなくてよい |
| `pcexpect` | 記者予想（印・進入予想・自信度）。選手コメントの代わりに使える |
| `data/stadium?jcd=NN` | **場ごとのコース別入着率・コース別決まり手・枠番別コース取得率・季節別・水質・干満差・レコード** |
| `race/index?hd=` | その日の開催場 |
| `race/pitreport` | 選手コメント。ただし **SG/G1 など一部レースだけ**（普通の開催では「表示対象レースではありません」） |

**取れないもの**：選手コメント（大半のレース）、「整備力」という指標そのもの。
整備力は `beforeinfo` の**部品交換欄**（ピストン/リング/電気/キャブ/シリンダ/クランクシャフト/ギヤケース/
キャリアボデー/プロペラ新）を節を通して集め、**交換後にモーターの成績がどう動いたか**で自前算出する。

**叩きすぎると 200 のまま「システムエラー」ページ（約14KB）が返る。**
`tools/lib/bt.mjs` の `get()` が900ms間隔・単一スレッド・キャッシュ・エラーページ検出を担保している。
並列取得はしない。開催前に取った空ページを長いTTLで抱えないよう `freshTtl()` を使う。

### 全24場のコード
```
01桐生 02戸田 03江戸川 04平和島 05多摩川        （関東）
06浜名湖 07蒲郡 08常滑 09津                     （東海）
10三国 11びわこ 12住之江 13尼崎                 （近畿）
14鳴門 15丸亀                                   （四国）
16児島 17宮島 18徳山 19下関                     （中国）
20若松 21芦屋 22福岡 23唐津 24大村              （九州）
```

### ファイル構成
```
boat.html                    ボートレース版のアプリ（1ファイル・依存ゼロ。NKBOAT マーカーに today.json を埋め込む）
data/boat/
  results.jsonl              K を1レース1行にしたもの（.gitignore。222MB。キャッシュから作り直せる）
  programs.jsonl             B を1レース1行にしたもの（.gitignore。247MB）
  stadium.json               ★全24場の公式集計（コース別入着率・決まり手・枠→コース・季節別・水質）— コミット対象
  tide.json                  ★気象庁の潮位表（毎時潮位・満潮・干潮。干満差のある場の最寄り観測点）— コミット対象
  index.json                 ★選手・モーター・場・水面条件の指数 — コミット対象
  index.train.json           ★先読みを避けた学習用の指数（検証開始の前日まで）— コミット対象
  model.json                 ★条件付きロジットの係数と検証結果 — コミット対象
  backtest.json              ★的中率・回収率 — コミット対象
  today.json                 ★今日・明日の全場のレースと予測（boat.html 埋め込み用）— コミット対象
  preds.jsonl                ★締切が過ぎたレースの予想の記録（追記のみ・回収率の算出用）— コミット対象
  results.json               ★日別・場別の的中率と回収率（top.html 埋め込み用）— コミット対象
  live.<日付>.json           ★当日の直前情報と最新オッズ — コミット対象
  odds_live.jsonl            ★締切前オッズの記録（追記のみ）— コミット対象
  before.jsonl               ★過去の直前情報の無作為標本 1,640レース（部品交換などの検証用）— コミット対象
tools/
  lib/bt.mjs                 取得共通（od2 の LZH 展開／Web のレート制限とキャッシュ／全24場の表）
  lib/od2.mjs                K・B のパーサ（bslice でバイト位置を切る）
  lib/web.mjs                beforeinfo・oddstf・odds3t/3f のパーサ。風向コード→方位の対応表（WIND_OFFSET）
  lib/origex.mjs             各場の独自サイトのオリジナル展示データ（一周・まわり足・直線）の URL 表とパーサ（13場）
  lib/tide.mjs               潮位表から締切時刻の潮の局面（上げ／下げ・高さ）を引く tideOf()
  lib/bload.mjs              K と B の突き合わせ、モーター世代、「そのレースより前」だけで作る調子・今節の指標
  lib/bfeat.mjs              条件付きロジットの特徴量（pre / ex の2レベル）
  fetch_od2.mjs              K・B を日付範囲で取り込む
  fetch_stadium.mjs          全24場の場データ
  fetch_tide.mjs             気象庁の潮位表（推算値）→ tide.json（干満差のある12場の最寄り観測点、年1回）
  fetch_live.mjs             当日の直前情報とオッズ、締切後のレース結果（launchd: com.boat.live が1分おきに呼ぶ）
  fetch_before_hist.mjs      過去の直前情報を無作為標本で取る → before.jsonl
  build_boatdb.mjs           選手・モーター・場・水面条件の指数 → index.json
  fit_boat.mjs               条件付きロジットの当てはめ → model.json（BT_FIT_DROP で切り分け）
  backtest_boat.mjs          買い方ごとの的中率・回収率 → backtest.json
  build_boat.mjs             今日・明日の全レースにモデルを当てる → today.json。締切が過ぎたレースの予想を preds.jsonl に記録
  build_boat_results.mjs     preds.jsonl × K で日別・場別の的中率・回収率 → results.json
  refresh_boat.mjs           変化があれば build_boat → embed → commit/push（launchd: com.boat.refresh）
  boat_check.mjs             DOM スタブで boat.html の全レースを描画して検査（ブラウザ不要）
```

### パイプライン
```bash
BT_FROM=20230901 BT_TO=20260910 node tools/fetch_od2.mjs   # 3年ぶん。1日2リクエスト・約35分
node tools/fetch_stadium.mjs                               # 全24場の場データ（月1でよい）
node --max-old-space-size=8192 tools/build_boatdb.mjs      # 指数
node --max-old-space-size=10240 tools/fit_boat.mjs         # モデル
node tools/backtest_boat.mjs                               # 検証
BT_LIVE_MAX=12 node tools/fetch_live.mjs                   # 当日（締切8分前のオッズと直前情報）
```

```bash
# boat.html への反映（モデルと指数ができていれば数十秒）
node tools/build_boat.mjs                                  # 今日・明日の全レースに予測を付ける → today.json
NK_EMBED_ONLY=boat node tools/embed_db.mjs                 # boat.html の NKBOAT に埋め込む
node tools/boat_check.mjs                                  # 全レースを DOM スタブで描画して検査
NK_REFRESH_NOPUSH=1 NK_REFRESH_FORCE=1 node tools/refresh_boat.mjs   # 上の3つをまとめて（push なし）
```

**当日ぶんは launchd で自動化してある**（`sh tools/launchd/install.sh` が南関の2つと一緒に登録する）。
- `com.boat.live` … 1分おきに `fetch_live.mjs` を1周回（上限20ページ）。開催のない日・時間帯は「本日のレース」を見て何もせず終わる。
  暫定オッズは最初に1回取ったあと、**締切60分前に入ったら15分おきに取り直す**（`BT_ODDS_REFRESH` / `BT_ODDS_WINDOW`）。
  以前はこれが無く、深夜に取った暫定が締切8分前まで止まっていた。締切8分前のスナップショットを取ったあとは取り直さない
- `com.boat.refresh` … 3分おきに `refresh_boat.mjs`。直前情報・オッズ・番組表が変わっていれば
  `build_boat_results → build_boat → embed_db(boat) → commit / push`。1時間に1回、昨日〜明日の K・B を od2 から取り直す
  （B は前日夕方に順次公開されるので、明日の番組表はここで入ってくる。K は開催中に途中まで公開される）

**日別・場別の回収率（`build_boat_results.mjs` → `results.json` → TOP のボート面）**
- `build_boat` が**締切の過ぎたレース**の予想（◎〜の順・1着確率・3連単本線・そのときのオッズ）を `preds.jsonl` に1行ずつ記録する。
  南関と同じで、**後から作り直さない**のが回収率の信用の要。モデルはオッズを使わないので記録が締切より遅れても中身は同じだが、
  `late`（締切から何分後か）を残し、30分以上あとに記録したレース数を表に添える
- 買い方は `backtest_boat` と同じ（◎単勝／◎複勝／◎○2連単・2連複／3艇BOX3連複(1点)／3艇BOX3連単(6点)／4艇BOX3連複(4点)／基準の1号艇）
  に加えて、印から組む2連単・3連単（◎→○▲ など）と **AI の買い目**（確率そのものから組む：
  3連単 確率上位3／5／8点、2連単 確率上位3／5点、3連単 期待値1.0超＝オッズがあるときだけ・最大6点）。
  AI の買い目は `build_boat` が締切時点の組を `preds.jsonl` の `ai` に残し、`build_boat_results` がそれを精算する。
  期待値組は過去データにオッズが無いので backtest では測れない（日別の成績だけが実測）
- 払戻は K の `pay`（`win`/`place`/`ex2`/`qn`/`wide`/`ex3`＝3連単/`tri`＝3連複）。**`ex3` と `tri` を取り違えない**
- `boat.html` の場データにも「本日の成績」（その場のここまで）を1行出す

第2段（オッズとの合成）と期待値ベースの買い方は、ここで貯まる `odds_live.jsonl`（締切8分前の3連単120通り）
が無いと検証できないので、**止めない**こと。ログは `data/boat/live.log` / `refresh.log`、多重起動は `.live.lock` が防ぐ。

**od2 のキャッシュの落とし穴**：`getOd2()` は 404 を「開催なし」として空ファイルで覚える。
直近・未来の日付でこれをやると翌日の番組表が二度と取れなくなるので、直近4日は 30分で取り直す。
当日の K は開催中に**途中までの内容**で公開されることがある（1.4KB の途中版を実際に抱えた）ので、
中身があっても同じ扱い。

| 変数 | 既定 | 意味 |
|------|------|------|
| `BT_WAIT` | 900 | 取得間隔(ms) |
| `BT_FROM` / `BT_TO` | 直近90日 | od2 を取る日付範囲 |
| `BT_DB_FROM` / `BT_DB_TO` | 全期間 | 指数に使う期間（先読みを避けたいとき） |
| `BT_DB_SD` | 0.55 | 選手指数の事前分布の広さ（対数オッズ） |
| `BT_FIT_SPLIT` | 20260601 | 学習と検証を切る日付 |
| `BT_LEAD` | 8 | 締切の何分前にオッズを残すか |
| `BT_LIVE_MAX` | 12（launchd は 20） | 1周回で取るページ数の上限 |
| `BT_ODDS_REFRESH` / `BT_ODDS_WINDOW` | 15 / 60 | 締切 WINDOW 分前以内の暫定オッズを REFRESH 分おきに取り直す |

### 指数（`data/boat/index.json`）
3年ぶん（2023-09〜2026-09、167,009レース・1,738選手）が母集団。

| キー | 中身 |
|---|---|
| `base['場\|コース']` | 場×コースの基準（1着率・2連対率・3連対率・平均ST・決まり手の内訳） |
| `racer[登番].idx` | **実力**。`logit(p) = logit(基準) + θ` の θ をリッジ付きで当てはめた値 |
| `racer[].byC[コース]` | そのコースでの上振れ（本人の `idx` を事前分布に置いて縮小） |
| `racer[].byJ[場]` | 当地での上振れ（同上）＝当地巧者 |
| `racer[].stDev` | 平均STの全体平均からのズレ（マイナス＝速い） |
| `racer[].kim` | 1着の決まり手の内訳＝逃げ屋か攻め屋か |
| `racer[].inGain` | **前づけ癖**。枠番−進入コースの平均（＋が大きいほど内を取りに行く） |
| `racer[].fRate` | F率 |
| `motor[場][番号#世代]` | モーターの指数。**番号だけでは別個体が混ざる**ので「40日以上あいたら別世代」で切る |
| `venues[場]` | K データ実測のコース別成績・決まり手・距離・天候・平均風速/波高・安定板使用率 |

**検算に使えること**：級別ごとの平均 `idx` は A1 +0.56 / A2 +0.13 / B1 −0.56 / B2 −0.63 と
きれいに並ぶ（級別は指数の計算に使っていない）。`inGain` の上位は西島義則・今村暢孝・石川真二・
江口晃生といった前づけで知られる面々。**この2つが崩れたら指数の作り方を疑う。**

### モデル（`tools/fit_boat.mjs`）
南関側と同じ Plackett–Luce（1〜3着の並びまで使う条件付きロジット）を Adam で当てはめる。
**2レベル作る**。どちらも3年ぶんの K で学習できる（K に展示タイムと進入コースが入っているため）。

- **pre** … 番組表と天候だけ。発走の数時間前に出せる。進入は**枠なり前提**で、
  場の「枠番別コース取得率」（`stadium.json` の `take`）で加重する
- **ex** … 直前情報が出たあと。**進入コースが確定**し、展示タイムが使える

**進入コースを one-hot にするのは ex だけ。** pre で進入を確定として扱うと先読みになる。

**先読みを避ける**：`index.json` を全期間から作ると、検証期間の結果が選手指数に入る。
必ず `BT_DB_TO=<検証開始の前日> BT_DB_OUT=data/boat/index.train.json` で学習専用の指数を作り、
`BT_FIT_DB` でそれを読ませる。**この違いは小さくない**（1着的中 58.7% → 55.8%、上位3艇 88.9% → 86.6%）。

### 検証結果（学習 2023-09〜2026-05 の 151,120レース／検証 2026-06〜09 の 15,791レース）

予測精度（`index.train.json` を使った、先読みのない数字）

| | logloss | 1着的中 | 上位3艇に勝ち艇 |
|---|---|---|---|
| 進入コースだけ | 1.409 | 54.8% | 81.4% |
| pre（番組表＋天候） | 1.242 | 55.8% | 86.6% |
| **ex（＋直前情報）** | **1.224** | **56.6%** | **87.4%** |

回収率（1点100円、ex、15,791レース）

| 買い方 | 的中率 | 回収率 | | 基準 | 的中率 | 回収率 |
|---|---|---|---|---|---|---|
| ◎単勝 | 56.6% | **92.1%** | 対 | 1号艇の単勝 | 54.4% | 89.8% |
| ◎複勝 | 75.3% | **94.7%** | 対 | 1号艇の複勝 | 71.9% | 92.9% |
| 本命3艇BOX 3連複 | 24.9% | **82.1%** | 対 | 1-2-3の3連複 | 17.7% | 78.3% |
| 本命4艇BOX 3連複 | 55.5% | **76.5%** | 対 | 1-2-3-4BOX 3連複 | 43.5% | 71.2% |
| ◎○の2連単1点 | 22.4% | 82.5% | | | | |
| ◎1着固定 3連単6点 | 42.8% | 77.4% | | | | |

**必ずベンチマークと並べて見ること。** ボートは1号艇の1着率が54.4%あるので、
「1号艇の単勝を毎回買う」だけで89.8%になる。モデルの92.1%はそこから+2.3ポイントで、
**全券種で基準を上回ってはいるが、まだ100%には届いていない**。

絞り込み（本命の予想勝率で層別、ex、15,791レース）

| 本命の予想勝率 | レース | 実際の勝率 | 単勝回収 | 複勝回収 |
|---|---|---|---|---|
| 0.0〜0.4 | 5,238 | 39.6% | 91.0% | 93.7% |
| 0.4〜0.5 | 3,862 | 54.0% | 91.2% | 93.3% |
| 0.5〜0.6 | 3,280 | 65.9% | 92.8% | 95.8% |
| 0.6〜0.7 | 2,275 | 75.1% | 94.2% | 96.3% |
| 0.7〜0.8 | 962 | 79.0% | 93.5% | 96.7% |
| 0.8〜1.0 | 174 | 84.5% | **101.0%** | 99.5% |

**絞るほど上がるが、100%を超えるのは最上位の174レースだけ**で標本が足りない。
南関側と同じく、段位は「買う・見送る」の機械的な根拠ではなく金額の強弱の参考値まで。
なお実際の勝率が予測をやや上回っている（0.4〜0.5 区分で54%）ので、確率は少し控えめに出ている。

### boat.html（載せ替え済み。2026-09-15 に白地・場ページ型へ組み替え）
`tools/build_boat.mjs` が今日・明日の全場のレースに **index.html と同じ考え方で「ツール側で計算した予測」**を付け、
`embed_db.mjs` が `NKBOAT` マーカーに埋める。ページ側では確率を計算しない（`bfeat.mjs` の写しをブラウザに置かない）。

**画面の作り（1ファイルのまま「場のページ」として見せる。場ごとにファイルは分けない）**
- 配色は白地（`--bg #eef1f5`／紙 `#fff`／紺 `--navy #0f2f5f`／赤 `--red`）。競輪公式サイト風のリスト型。**3D のキャンバスだけ暗い**
- 上部（sticky）：日付タブ → 開催場の3行（モーニング／日中／ナイター。チップに **n日目/N日間** と次の締切）→
  **場の見出し `#vhead`**（節名・n日目／N日間・初日／予選最終日／準優勝戦／優勝戦の札・本日の傾向・**次の締切**）→ 1〜12R のチップ
  （終わったレースは着順 `1-6-4` を出す）→ セクションタブ
- 締切は `SOON`（10分前）以内で赤字＋「あと n 分」。15秒ごとに描き直す（`renderVenues / renderVhead / renderRaces`）
- セクションは4つ：**予想**（左に水面気象・進入展示・予測の段階、右に予想表〜読みのポイント）／**節間の結果**／**場データ**／**3D再生**。
  PC は上のタブ、スマホ（900px以下）は下タブ。`?tab=log|venue|3d`（予想は無指定）
- **節間の結果**（`v.log`）：`build_boat` の `meetLog()` が、その節の初日から当日までの各レースについて
  結果（K、無ければ公式サイトの結果ページ）と**締切時点で記録した予想**（`preds.jsonl`。後から作り直さない）を1行にし、
  的中と払戻を `lib/bsettle.mjs`（日別の成績と同じ式）で付ける。日ごとの集計（◎1着・上位3艇・◎単勝／◎複勝／3艇BOX3連複／AI上位3点の回収）も持つ。
  節の日程は `live.<date>.json` の `meet.dates`、無ければ番組表の「第n日」で遡る。1レース 200 バイト前後、全場で 150KB ほど
- 当日の終わったレースには `race.result`（着順・決まり手・払戻・的中）を付け、予想表の上に「結果」箱、印の横に着順を出す
- `lib/bsettle.mjs` は `build_boat_results.mjs` と `build_boat.mjs` の共用。**精算の式をここ以外に書かない**
- `refresh_boat.mjs` は `results.jsonl` と昨日の `live` も変化の対象に入れてある（K の取り直しで節間の結果が更新される）
- `boat_check.mjs` は要素チェックに `vhead / races / logTitle / btBox` を足し、節間の結果の行数＝出走レース数、結果の着順が3つあることを見る
- **予想表にモーター・ボートの2連率**（M番号・今節の番組表の値・棒。40%以上を緑、25%未満を橙）、**モーター指数**（3年の実測の上振れ）と直近の伸び、
  **展示タイム（順位・棒）・展示ST（棒）**を列で出す。左には「展示・機力の比較」表（6艇を横並びで棒）。スマホは 級・展示ST・ボート・3連 を落として横スクロール
- **オリジナル展示データ（一周・まわり足・直線タイム）**は boatrace.jp には無く、各場の独自サイトから取る（`tools/lib/origex.mjs`。2026-09-15 に24場を調査）。
  **取れる13場**：XOOPS 系（`/modules/yosou/…?day=YYYYMMDD&race=R&…&if=1` の iframe 中身を直接 GET）＝01 桐生（半周）・06 浜名湖・08 常滑・09 津・10 三国・11 びわこ・
  13 尼崎（直線なし）・14 鳴門・18 徳山（直線なし）・21 芦屋・22 福岡・23 唐津、TBK 系（`/asp/kyogi/{場}/pc/…RR.htm`。URL に日付が無く当日のみ）＝04 平和島・12 住之江（直線なし）。
  **取れない場**：02 戸田・03 江戸川・05 多摩川・07 蒲郡・17 宮島・19 下関・24 大村（サイトに無い）、15 丸亀・16 児島（TBK 系の `st02RR.htm` はあるが非開催日で列を確認できていない）、20 若松（名前解決できず未確認）。
  値は場ごとに計測区間が違う（桐生は半周、尼崎のまわり足は11秒台）ので**場をまたいで比べない**。
  `fetch_live` が直前情報の公表後に1レース1ページ取り（値が `-.--` なら5分あけて再試行）、`live.<date>.json` の `races[].orig` に入れる。
  `build_boat` が艇に `oLap / oTurn / oStr`、レースに `orig.labels`、場に `origSrc`（取れる場か）を付け、読みのポイントに「一周最速 …」を足す。
  画面は予想表の「独自展示」列（値と6艇中の順位）と「展示・機力の比較」の列。XOOPS 系の列クラスはサイトごとに違う（`cells`）ので、崩れたら保存済みの実ページで `parseOrigEx` を当て直す
- **モーター一覧（場データのタブ）**：`build_boat` の `motorList()`。その節の番組表に出た全モーターを、今期2連率（番組表の値＝入れ替え後の累計）・
  3年の実測指数（同じ番号・同じ世代）・節間の成績（K の着順をモーター番号で集計）・日ごとの着順・本日の割り当て・節間に乗った選手で並べる。
  エース＝今期2連率の1位（上位2・3も札）。並び替えは 今期2連率／3年指数／節間の成績／番号順。全場で約170KB
- **潮位**：`tools/fetch_tide.mjs` が気象庁の潮位表（推算値・年1ファイル `…/tide/suisan/txt/{年}/{地点}.txt`、1行1日＝毎時24×3桁＋満潮4×7桁＋干潮4×7桁）を
  干満差のある12場ぶんの最寄り観測点で取って `data/boat/tide.json`（約570KB、コミット対象）。場→観測点は `STATION`（下関・若松＝門司、鳴門＝小松島、大村＝長崎は参考値）。
  `build_boat` が場ごとに `tideDay`（毎時潮位・満潮・干潮）を付け、ページ側で締切時刻の潮位・上げ／下げ・次の満潮を出す（`tideAt`）。
  場の見出し・予想の「潮」行・左の「潮」・場データの1日の潮位グラフ（各レースの締切を点線で重ねる）。`refresh_boat` が1日1回 `fetch_tide` を呼ぶ（キャッシュ200日）

- 日付タブ（今日／明日）→ 場（**モーニング／日中／ナイターの3行**。1Rの締切が 10:30 前＝モーニング、14:30 以降＝ナイター。
  行ごとに ▼ で畳め、状態は localStorage）→ 1〜12R
- 場のチップには「n日目」「次のレースと締切」と傾向バッジ（堅／並／荒）。締切10分前以内の場は赤字（15秒ごとに更新）
- **本日の傾向**（`build_boat` の `trendOf`）：当日の K（開催中に途中まで公開される）が3R以上あれば
  1コース勝率・3連単平均配当・万舟率から 堅め／ふつう／荒れ気味（`ex3` が3連単、`tri` は3連複なので取り違えない）。
  無ければ予想の本命勝率の平均で 堅め／ふつう／荒れ含み。TOP の「場ごと」も同じ値を出す
- 直前情報が出たレースは **ex**（進入確定・展示タイムあり）、それ以外は **pre**（番組表と気象だけ）。
  左パネルの「予測の段階」で ex のレースを pre に切り替えて見比べられる
- pre の気象は「同じ場の最新の直前情報」の値を推定として使う（出典を表示）。何も無ければ**気象の項は 0**
  （`bfeat.mjs` は `race.wind == null` のとき水面条件の表を引かない。0m として引くと無風時の偏りが乗る）
- 1〜3着の並びは Plackett–Luce を6艇120通りで厳密に足し上げる（`plackettLuce()`）。
  3連単の上位12・3連複の上位6・各艇の1着/2連/3連率はここから
- **3D 再生は同じモデルからの標本**。`sampleRace()` が `U_i + Gumbel` の大きい順で着順を引く（Gumbel-max は PL の標本）。
  ST はスタート展示の値（無ければ平均ST）に揺らぎを足したもの。旧版の物理シミュレータ（`derive`/`simRace`）は廃止
- オッズは締切前スナップショット（`snap`）を優先、無ければ暫定。3連単は120通りの実オッズで **期待値＝確率×オッズ** を出す
- 「おすすめ」は `backtest.json` の表から **検証実績（的中率・回収率・1号艇買いとの比較）を必ず並記**する。
  回収率100%の買い方は無いので、その但し書きを外さないこと
- 「根拠」は係数×特徴量を コース／実力／当地／ST／モーター／調子／水面／展示／その他 に束ねた値（`build_boat` の `contrib()`）
- `node tools/boat_check.mjs` が全日・全場・全レースの描画と「勝率の和＝1」「艇数6」を見る
- スマホ（900px以下）は下タブ 予想／結果／場／3D。**最初に開くのは「予想」**（3D は選んで見る）。
  `?tab=log|venue|3d` で指定でき、レースを切り替えると URL に残す。予想表は級と展示の列を落として6列にし、横スクロールできる
- **埋め込みは軽く保つ。** 艇は `boatCols`（列名）＋配列で持ち、`boat.html` の `unpackBoats()` が読み込み時に
  オブジェクトへ戻す。使わない項目（艇番号・母集団の勝率・決まり手の内訳など）は落とし、確率は3桁・指数は2桁に丸める。
  1日ぶん（今日＋明日、約240R）で boat.html 約820KB。蓄積するのは `data/boat/` 側で、ページは常に今日・明日だけ
- TOP（`top.html`）のボート面は `build_boat.mjs` が同時に書く `data/boat/top.json`（1レース数百バイト）を
  `NKBOATTOP` に埋める。`refresh_boat.mjs` は `top.html` もコミット対象に含める

### 風向の対応表（`lib/web.mjs` の `WIND_OFFSET`）
直前情報の風向アイコン（`is-wind1〜16`、17＝無風）は**水面図基準の相対方位**で、K の風向（北・北東…の絶対方位）と
場ごとに回転がずれている。標本1,640レースで突き合わせ、16方位のうち一致が最大になる回転量を場ごとに求めた
（一致率 69〜98%、風速2m以上で判定）。`windCompass(jcd, code)` が K と同じ8方位文字列に直す。
**この表が無いと、当日の予測で `wDir`（係数 0.39、全体8位）が効かない。** 標本を増やしたら求め直してよい。

### 競走得点（節間の得点率）— 2026-09-14 追加
公式サイトの出走表・開催情報には得点の表が無い。**番組表の「今節成績」（着順の並び。例 `2 163354`）から自前で計算**する
（`lib/bload.mjs` の `setuPoints` / `attachPoints`）。得点は一般戦の 10-8-6-4-2-1 点、F/L/S/K など事故は0点で走数に数える。
G1・SG や準優の加点は入れていないが、同じ節の全員に同じ加点が乗るので**節内の順位は変わらない**。
その日・その場の出走選手全員で順位を付け、準優進出ボーダー（42人前後の開催は18位、少人数は12位）との差を出す。
特徴量は `ptRate`（得点率−5.5）／`ptGap`（ボーダー差）／`ptRank`（節内順位）。初日は今節成績が無いので 0。
根拠と読みのポイントに「得点率 7.33（5位/42人・準優ボーダー+0.80）」「準優ボーダー争い：…」と出す。
**結果（2026-09-14、検証 16,403R）**：`ptRank`（節内順位）の係数 0.226 で全体9位。ex の logloss 1.210 → **1.204**、
1着的中 56.6% → 56.8%、上位3艇 87.4% → 87.5%。回収率は ◎単勝 92.3%／◎複勝 95%／3艇BOX3連複 82.1%（1号艇の単勝 89.6%）。
得点率そのもの（`ptRate`）やボーダー差より**節内順位**が効く。

### 勝ち上がり条件と勝負駆け（2026-09-14 追加、表示のみ）
- 節の日数と今日が何日目かは `fetch_live` が開催情報ページ（`raceindex?jcd=&hd=`）の日程タブ（初日／２日目／…／最終日）から取り、
  `live.<date>.json` の `meet[jcd]` に持つ（`lib/web.mjs` の `parseRaceIndex`）
- `build_boat` がレースを 予選／準優勝戦／優勝戦／一般（敗者戦）に分け、予選最終日（準優は最終日の前日なので、開催日数に関わらず days−2 日目。4日開催なら2日目）を判定する。
  準優・優勝戦がある日の残りのレースは、名前が「サンライズ」「選抜戦」でも勝ち上がりに関係ない一般戦として扱う
- 予選の各選手について「残りの予選走数」（この日の残り＋残りの予選日×1日の走数）と「準優ボーダーに届くのに残りで必要な平均点」を出し、
  当確圏（全部6着でもボーダー以上）／要好走（4〜6点＝3着前後が要る）／**勝負駆け**（6〜10点＝2着以上か1着が要る）／厳しい（10点超＝届かない）を付ける。
  ボーダーは「今の18位（または12位）の得点率」で固定している（本当は他の選手の結果で動く）
- 表示：予想パネル頭に「4日目／6日間 予選最終日（準優進出は上位18人）」、艇名の横に赤の「勝負駆け」バッジ、読みのポイントに一覧
- 準優勝戦は「上位2着までが優勝戦へ」と一律に出している（開催によって1着のみのこともある）
- モデルの特徴量には入れていない（節の日数の履歴が無い）。効かせるなら過去の番組表から「その節の最大日数」を出して足す

### その日のここまでの結果を後半のレースに効かせる（2026-09-14）
- **結果の取り方**：K（od2）は開催中1時間おきにしか取り直さないので、`fetch_live` が締切6分後から公式サイトの
  レース結果ページ（`raceresult?rno=&jcd=&hd=`）を取る（`lib/web.mjs` の `parseRaceResult`：着順・進入コース・ST・決まり手・
  払戻・気象。未確定なら4分あけて再試行）。`live.<date>.json` の `races[].result` に入る。1周回で取る結果は直前・オッズの上限＋6ページまで
- **時点指標**（`lib/bload.mjs` の `makeRolling`）に当日ぶんを足した。`pushRace(r, 1着コース, 決まり手)` を各レースのあとに1回呼ぶ。
  `read()` は **番号が若いレースだけ**を見る（`x.r < r.r`）ので、`build_boat` は当日の終わったレースを先に全部流し込んでよい
  | 指標 | 中身 |
  |---|---|
  | `dayIn1` | その日の同じ場で、1コースの1着率が場の基準からどれだけズレているか（k=3 で縮小） |
  | `dayOut` | 1着艇のコース番号の、期待値からのズレ（＋＝外が来ている） |
  | `dayMak` | まくり・まくり差しの割合の 0.30 からのズレ |
  | `todayRel` / `todaySt` | その選手の当日ここまでの走り（相対着順−0.5、ST） |
  特徴量（`bfeat.mjs`）では場の値をコースと掛けて艇ごとにする（`dayIn1 × (1コース確率 − 1/6)`、`dayOut × (期待コース − 3.5)`）
- **先読み対策**：学習（`loadRaces`）は K を日付・場・レース番号順に流すので、同じ日の後のレースは入らない。10日ぶんで確認：
  「今日インが好調（dayIn1 > +0.05）」の後半レースで1コースの勝率 62.8%、不調（< −0.05）で 54.6%
- **build_boat**：当日の結果は K（`results.jsonl`）と公式サイト（`live.json`）を r で突き合わせて K 優先で使い、`pushTodayResults` で
  時点指標へ、`TODAY_RES` で「本日の傾向」（堅め／荒れ気味）へ。読みのポイントに
  「本日ここまで7R：1コース1勝（14%・ふだん58%）・まくり系6本 → インが弱い・外のコースが来ている」と「本日すでに走った艇」を出す。
  根拠のグループに「当日」を足した（`groups` はデータ側から渡すので boat.html の変更は不要）
- **モデルに無い特徴量は 0 で動かす**（`betaOf`）。特徴量を足した直後に refresh_boat が止まらないようにした。
  逆（モデルにあって bfeat に無い）は並びずれの恐れがあるので止める
- **当てはめ結果（2026-09-14、学習 151,120R／検証 16,522R）**：ex logloss 1.202（前 1.204、検証集合が少し違う）、1着的中 56.9%、上位3艇 87.6%。
  係数は小さい（`dayIn1` 0.03／`dayMak` 0.07／`todayRel` 0.05／`todaySt` −0.03）。回収率は ◎単勝 92.0%／◎複勝 94.9%／3艇BOX3連複 81.2%（1号艇の単勝 89.6%）で横ばい。
  **「今日はインが弱い」は読みのポイントとしての価値のほうが大きく、モデルの数字はほとんど動かない。**
  同じ検証集合で外して比べた（`BT_FIT_DROP=dayIn1,dayOut,dayMak,todayRel,todaySt`、ex）：外すと logloss **1.203**／1着的中 56.8%／上位3艇 87.6%。
  **差は 0.001**。入れておいて害は無いが、精度の材料としては数えない

### まだやっていないこと
- **第2段（オッズとの合成）**。K ファイルには当たった組の配当しか無いので、
  過去データでは市場の評価を再現できない。`fetch_live.mjs` が貯める `odds_live.jsonl`
  （締切8分前の3連単120通り）がたまってから測る
- **期待値ベースの買い方**。同上。ボートは3連単の全120通りのオッズが公式に出るので、
  貯まれば南関より正確に測れる
- **部品交換・チルト・調整重量の価値**。`before.jsonl`（標本1,640レース）で「モデルの予測に対する残差」を見る。
  価値があれば本取り（3年ぶん16万リクエスト・40時間超）を検討する。それまで直前情報ページは当日ぶんしか取らない
### 着順の段階ごとの温度（`lib/bpl.mjs`）— **必ず通すこと**
1〜3着の並びを同時に当てはめた PL は、**1着確率が系統的にずれる**。検証期間で 1コースの予測45.2%に対して
実際54.8%、較正ビンでも 予測45%→実際54%、55%→66%、65%→75% と本命側を低く、穴側（5%→3.4%）を高く出していた。
1号艇のように「勝つか沈むか」の艇は2・3着の尤度で強さを引き下げられるため（IIA が成り立たない）。

対処は β を共通のまま、**1着は τ1、2着以降は τ2 の温度**で `exp(τ·U)` を取ること。
`fit_boat.mjs` が学習データの尤度で τ1・τ2 を黄金分割で決めて `model.json` の `tau` に入れる
（pre: τ1 1.44／τ2 0.74）。効果は pre で logloss 1.236 → **1.223**、新しい特徴量11個の合計より大きい。
`build_boat` / `backtest_boat` / `check_before` / `boat.html` の標本抽出はすべて `lib/bpl.mjs` の
`plackettLuce(U, tau)` を通す。**素の softmax を書かない。**
`BT_FIT_BETA=<model.json>` を渡すと β を再利用して温度と検証だけをやり直せる（数分）。

### 特徴量の切り分け（2026-09-10、ex・250エポック・検証 15,791R・温度なし）
`BT_FIT_DROP` で列を落として同じ手続きで当てはめ直した結果。

| 構成 | logloss | 1着的中 | 上位3艇に勝ち艇 |
|---|---|---|---|
| A 風・波・整備力・調子をすべて外す | 1.227 | 56.5% | 87.3% |
| B 粗い風・波（`windC`/`waveC`）だけ | 1.227 | 56.5% | 87.3% |
| C 場ごとの条件テーブル（`wDir`/`wSpd`/`wWave`）だけ | 1.225 | 56.5% | 87.3% |
| D 風・波は両方（調子なし） | 1.225 | 56.5% | 87.3% |
| E D＋整備力（`tune`/`mUp`） | 1.225 | 56.5% | 87.3% |
| F D＋調子・今節（`form`…`setuRuns`、整備力なし） | 1.223 | 56.6% | 87.4% |
| G 全部（いまの構成、400エポック） | **1.223** | **56.6%** | **87.4%** |

- **整備力は効かない**（E＝D。係数も `tune` −0.008、`mUp` 0.000）。部品交換から作った「交換後にモーターが良くなるか」の指標は
  予測に足せる情報を持っていなかった。寸評に出す価値も薄い
- **場×風向の表**は粗い風・波より良い（C＞B）が、伸びは 0.002。粗い風・波は表があれば不要（D＝C）
- **調子・今節**は 0.002。`form` の係数は −0.161 で**符号が逆**（本人の平常値で縮小した残差が実力指数と相関し、
  南関の騎手の調子で踏んだのと同じ現象）。グループとしては僅かに効くので残しているが、
  「調子が良いから買う」という読みには使えない。寸評では `form` 単体を強調しない
- **合計でも 1.227 → 1.223**。ボートは進入コースと選手の実力でほぼ決まっており、
  直前情報を足しても伸びしろは小さい。次に伸ばすなら特徴量追加より**第2段（オッズとの合成）**
- 10エポックの途中の値（logloss 1.199）が収束後（1.223）より良かった。学習に寄りすぎている気配があるので、
  次に当てはめるときは `BT_FIT_L2` を上げるか、検証 logloss が最良のエポックで止めることを試す

### 進入固定・今節F・潮・節の段階・安定板を足した結果（2026-09-15、学習 151,120R／検証 16,570R）
「すぐ足せる5つ」を同じ分割で 新特徴量なし／あり の2本を当てはめて比べた（`BT_FIT_DROP` で切り分け、`index.train.json` で先読み回避）。

| | pre logloss | pre 1着 | ex logloss | ex 1着 | ex 上位3艇 |
|---|---|---|---|---|---|
| なし（基準） | 1.2149 | 56.37% | 1.2016 | 56.88% | 87.64% |
| あり | 1.2149 | 56.36% | 1.2015 | 56.86% | 87.67% |

- **ほぼ変わらない**（ex で logloss −0.0002）。回収率も ◎単勝 92%／◎複勝 94.9%／3艇BOX3連複 81.4% で同じ
- 係数：`phSemiC` −0.15（準優勝戦は内が堅い）、`setuF` +0.02〜0.05（F持ちは僅かにプラス＝攻める選手が多い）、`tideC`/`tideLvlC` は 0.00〜0.01（**潮の局面は効かない**。最寄り観測点の推算値で場の水面とは時刻がずれる）、`phFinalC` −0.02〜−0.04
- **進入固定**は pre の進入を枠＝コースで確定させる作り（`race.fixed`）。対象が193R/7,040R と少なく、全体の数字には出ない
- **安定板**は od2 の K・B に記載が無く学習できない（`stable` は全レース null だった）。`parseBefore` が直前情報ページの「安定板使用」を拾って `live.json` に残すようにしたので、表示と今後の検証用
- モデルは「あり」を本番に載せた（害は無く、特徴量が揃っているほうが今後の検証で使い回せる）。潮・進入固定・F持ち・安定板は**表示の価値が主**
- 潮位は `BT_TIDE_YEARS=2023,2024,2025,2026 node tools/fetch_tide.mjs` で学習期間ぶんを取ってある（`tide.json` 約2.3MB）。`lib/tide.mjs` の `tideOf(jcd, date, close)` が局面（上げ/下げ）と高さを返す

### 級別ボーダー争い（2026-09-15 追加）
級別（A1/A2/B1/B2）は半年ごとの審査期間（前期 5/1〜10/31、後期 11/1〜4/30）の勝率・事故率・出走回数で決まる。
`lib/bload.mjs` の `makeRolling` が期間ごとの着順点（1着10・2着8・3着6・4着4・5着2・6着1、準優+1・優勝戦+2 の概算。SG/G1 の加点は未反映）と
事故点（F・L・失格・転覆 10、不良航法・待機違反 7 の概算）を**時点つき**で積み、`kyu`（勝率・2連対率・事故率・走数）として各艇に付ける。
ボーダーは「基準値（A1 6.20／A2 5.40）」と「その時点の分位点（上位20%／40%、出走20回以上の選手）」の大きいほう（1日1回計算）。
`bfeat.mjs` の `kyuGapOf(kyu, grade)` が、いまの級ごとに見るべきボーダー（A1 は維持、A2 は昇格か維持の近いほう、B1 は A2 昇格）との差を返す。
- 表示：艇名の横に **昇格争い**（ボーダー下 0.20 以内）／**降格危機**（維持ボーダー上 0.15 以内）／**昇格圏**（昇格ボーダー上 0.15 以内）の札。期末75日前からか、差 0.10 以内なら常時。
  根拠に「今期勝率 6.57（88走・2連対56%・事故率0.11）｜A1維持のボーダー 6.21 まで +0.35（期末まで47日）」、読みのポイントに一覧
- 特徴量 `kyuNear`（ボーダーに近いほど・期末が近いほど）／`kyuBelow`（下から狙う側）。**当てはめでは係数 +0.04／0.00 でほぼ効かない**
  （検証 16,570R：ex logloss 1.2015 → 1.2015、1着的中 56.9→56.8%）。検証期間（6〜9月）に期末が入っていないので、10月末を含めて測り直す価値はある
- `build_boat` は期の初日まで結果を流し込む（`ROLL_FROM`＝直近100日と期首の早いほう）

### 2着・3着の段階モデル（2026-09-15）— **回収率に直結する改善はここ**
PL は「1着の強さ」を2着・3着にも同じ式で使う（温度だけ変える）。実際は **1着の決まり方で2着の分布が変わる**
（1コースが逃げれば2着は2・3コースの差し、まくりなら外や残った内）。そこで
- 1着：第1段のモデル（温度 τ1）
- 2着：勝者 w を所与にした5択の条件付きロジット。特徴量は `cz2`＝P(2着コース｜1着コース) の対数オッズ（学習データの6×6表）、
  `u`＝第1段の強さ、`adjIn/adjOut`＝勝者のすぐ内／外、`stDiff/rDiff/exDiff`＝勝者との ST・実力・展示の差
- 3着：1・2着を所与にした4択。`cz3`＝P(3着コース｜1着コース)、`u`、`adjW/adjS`（1着・2着に隣接）、勝者との差
式は `lib/bpl.mjs`（`stage2X / stage3X / stagedPL / raceProbs`）に一元化。`tools/fit_boat_stage.mjs` が model.json の `stage` に書く
（**第1段の β を変えたら必ず回し直す**。`u` が β に依存する）。`build_boat` / `backtest_boat` は `stage` があれば段階モデル、無ければ PL。
3D の標本は `tri120`（120通りの千分率。使う段階だけ埋める）から1〜3着を引き、残りは Gumbel。

結果（検証 16,570R、ex）：2着 logloss 1.385 → **1.360**、3着 1.281 → 1.268、2連単本線 22.4 → **23.3%**、
3連単本線 9.4 → **9.9%**、上位3点 22.9 → 23.6%、5点 33.0 → 33.8%、8点 44.3 → 45.3%。
backtest でも AI 3連単 上位3点の的中 22.9 → 23.7%、上位8点 44.1 → 45.0%。**ただし回収率は 81% 前後で横ばい**
（増えた的中は本命側の低配当）。係数：2着は `u` 0.50／`cz2` 0.40／`rDiff` 0.40／`adjOut` 0.18／`stDiff` 0.17。

### 正則化（L2）のスイープと相互作用（2026-09-16）— **logloss 1.2015 → 1.185**
「10エポック時点のほうが良い」疑いから、早期終了と L2 を振った。学習 151,120R／検証 16,570R、`index.train.json`、相互作用あり。

| 設定 | pre logloss | ex logloss | ex 1着 | ex 上位3艇 |
|---|---|---|---|---|
| 現行（L2 2e-4、早期終了なし、相互作用なし） | 1.2149 | 1.2015 | 56.9% | 87.6% |
| A 早期終了（末尾15%で ep35）、相互作用なし | 1.217 | 1.207 | 56.6% | 87.6% |
| B 早期終了、相互作用あり | 1.216 | 1.206 | 56.6% | 87.5% |
| E 相互作用あり、L2 2e-4 | 1.214 | 1.202 | 56.8% | 87.5% |
| G L2 5e-3 | 1.211 | 1.198 | 56.9% | 87.5% |
| H L2 2e-2 | — | 1.193 | 57.0% | 87.6% |
| I L2 5e-2 | 1.201 | 1.189 | 57.0% | 87.6% |
| J L2 1e-1 | 1.199 | 1.186 | 57.1% | 87.6% |
| **K L2 2e-1（採用）** | **1.199** | **1.185** | 57.0% | 87.6% |
| L L2 5e-1 | 1.203 | 1.188 | 56.9% | 87.6% |

- **早期終了は逆効果**（学習を15%減らす損のほうが大きい）。`BT_FIT_ES=0` を既定にする
- **L2 を1,000倍（2e-4 → 2e-1）にして logloss が 0.017 下がった。** 係数はすべて小さくなる（cz 0.28、rIdxJ 0.23）が、温度 τ1 が 1.45 に上がって
  全体のスケールを補うので、実質は「特徴量どうしの相対の重みを平す」効き方。過学習していた
- 相互作用（実力・ST・モーター・展示×コース）は小さい（−0.02〜−0.04）。**`in1Idx`（いちばん内の艇の実力）は −0.106 で全体8位**：内が強いと外の艇は苦しい
- K の上で段階モデルを当て直した結果（検証 16,570R）：2着 logloss 1.358、3連単本線 9.9%、上位3点 24.2%、上位8点 45.9%、2連単本線 23.5%。
  backtest：◎単勝 的中 56.8 → 57.1%（回収 91.7%）、◎○2連単 22.2 → 23.0%、3艇BOX3連複 24.8 → 25.4%、AI 3連単 上位3点 23.7 → 24.3%、上位8点 45.0 → 45.8%。
  **的中率は全券種で上がったが回収率は 80〜82% で横ばい**（当たりが増えるのは本命側。市場は本命を正しく値付けしている）
- 当てはめは `--expose-gc --max-old-space-size=6144`。この Mac は RAM 8GB なので、fit_boat / fit_boat_stage はレースごとに行列化して元のオブジェクトを捨てる作りにした（ピーク 6GB → 2GB）。
  それでも他のアプリで常時 11〜14GB がスワップに出ていると数倍遅くなる。**夜間ジョブ（21:20／23:00／06:30）と重ねない**

### 守ること（ボート側）
- **指数はコース補正後で持つ。** 素の勝率・2連対率をそのまま実力に使わない
- **`bslice()` を通さずに K・B を切らない。** 漢字1文字=2バイトで桁が決まっている
- **F艇（フライング）の行は艇番までの空白が3つ**になる。`\s{1,2}` で正規表現を書くと
  F艇を丸ごと落とす（実際に40件落とした）。`\s+` にすること
- **3連単/3連複のオッズ表は rowspan で2着セルが縦に伸びる。** セルを頭から3つずつ取ると
  列がずれる（120通りのうち30通りしか拾えなかった）。行のセル数を列数で割って
  「1列3セルか2セルか」を決める
- **K ファイルには当たった組の配当しか載っていない。** 期待値ベースの買い方は
  当日の `odds3t` を貯めてからでないと検証できない
- **model.json と bfeat.mjs の特徴量は名前で突き合わせる。** `build_boat.mjs` は並びが食い違っていたら
  止まる（係数を位置で読むと、特徴量を足した直後に別の係数が当たる）。特徴量を変えたら必ず `fit_boat.mjs` を回し直す
- **「そのレースより前」の指標は必ず読んでから足す。** `makeRolling` は `read()` → `push()` の順。
  逆にすると当日の結果が当日の予測に入る（先読み）
